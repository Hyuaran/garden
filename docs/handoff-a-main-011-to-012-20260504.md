# Handoff - a-main-011 → a-main-012 - 2026-05-04(月) 23:20

## 引き継ぎ理由

**a-main-011 が 500 Internal Server Error / コンテキスト超過で対話継続不能**になったため、a-main-012 にクリーン引き継ぎ。

011 自体は正常稼働期間で大きな失敗なし（010 のような理解力リセットではない、純粋な技術的引き継ぎ）。

---

## 今やっていること（最重要：5/5 後道さんデモ目前）

### 1. Bloom Top + 経営状況プロト 最終調整中（a-bloom-002 で進行）

**直近の指示：main- No. 38 投下済**（`docs/dispatch-main-no38-bloom-002-navheader-and-logo-size-20260504.md`）

修正依頼 2 件：
- **# A**: nav-pages-header「Bloom」クリック 404 → `_proto/` 配下の壊れた相対 path（`../02_BloomTop/`）を `/bloom` に置換
  - 影響ファイル: `_proto/bloom-top/index.html`, `_proto/ceostatus/index.html`, `_proto/garden-home/index.html` 等
- **# B**: Topbar ロゴサイズ統一（Bloom Top 180×60 vs 経営状況 192×64 → 60px 高さに統一）

**a-bloom-002 からの応答待ち**。受領後は本番 `/bloom` に反映確認。

### 2. dispatch カウンター = 39（次は main- No. 39）

`docs/dispatch-counter.txt` = `39`。a-main-012 起動後、新規 dispatch を出す場合は **main- No. 39** から始める。

### 3. CLAUDE.md §19 トークン削減ルール 未コミット

011 セッション中に追加。Read offset/limit、Grep output_mode、Bash リダイレクト等の規律。a-main-012 でコミット & push 推奨。

---

## 次にやるべきこと

### 即時（5/5 デモ前 ≦ 18 時間）

1. **a-bloom-002 から main- No. 38 完了報告受領** → 本番 `/bloom` 確認
2. デモ向け最終視覚確認（Chrome MCP で `/bloom`、`/_proto/ceostatus/` 等を Claude 自身が回って差分検出）
3. 残課題があれば main- No. 39 として dispatch

### 5/5 後道さんデモ後（post-5/5）

memory `project_post_5_5_tasks.md` の 9 件を順次：
1. Root login UI 統一
2. Bloom 系 6 画面 Next.js 化 + Vercel + Supabase
3. claude.ai 後続 Step 1（遊び心 home）
4. claude.ai 後続 Step 2（全体ログイン）
5. claude.ai 後続 Step 3（Garden 開発進捗画面）
6. PR #85 レコード API 消失の追加報告判断
7. dev server long-running timeout 対策
8. Garden Sprout / Calendar モジュール構築
9. Workboard デザイン 3 案受領後の実装

---

## 注意点・詰まっている点

### 1. 011 で確立した運用ルール（継承必須）

| 項目 | 内容 |
|---|---|
| dispatch ヘッダー形式 | v4「main- No. NNN」（memory `feedback_dispatch_header_format.md`）|
| 視覚確認 | Chrome MCP で Claude 自身が実施（東海林さん丸投げ NG）|
| 議論前 | 既存実装を必ず確認 |
| 旧版データ | 削除禁止、`*.legacy-YYYYMMDD.tsx` 形式で併存 |
| トークン削減 | CLAUDE.md §19（Read limit / Grep files_with_matches / Bash リダイレクト）|

### 2. Bloom 認証問題（既知・対応済）

- Bloom は Forest と独立認証（`bloom_users` + 独自ロック解除）
- dev では BloomGate バイパス（commit `988efa5` で対応済）
- 本番では自動的に認証復活
- memory `project_bloom_auth_independence.md` 参照

### 3. Turbopack HMR 取りこぼし

- globals.css 大量変更時に HMR が反映しない事象あり
- dev server 再起動 + `.next` キャッシュクリア必須
- memory `feedback_turbopack_hmr_globals_css_unreliable.md` 参照

### 4. 未コミット 43 件

引き継ぎコミットで一括取り込み：
- CLAUDE.md（§19 追加、+274 行）
- docs/dispatch-main-no*.md 多数（main- No. 1〜38、bloom-002 への dispatch 履歴）
- docs/bloom-002-dispatch-v2-8a-*.md（Bloom 002 デバッグ調査メモ）
- docs/claude-chat-settings-update-*.md（Claude チャット設定更新履歴）
- docs/dispatch-counter.txt（=39）

---

## 関連情報

- ブランチ: `workspace/a-main-011`（このセッション）/ `workspace/a-main-012`（新セッション）
- 担当 a-bloom-002 ブランチ: `feature/bloom-6screens-vercel-2026-05`
- 関連 worktree: `C:\garden\a-bloom-002`、`C:\garden\a-main-011`、`C:\garden\a-main-012`（新規）
- 直近 PR: #76 sprout v0.1, #75 root Phase B specs, #71 tree Phase D plan v3

---

## a-main-012 起動後の最初のアクション

1. `pwd` で `C:\garden\a-main-012` 確認
2. `git status` で `workspace/a-main-012` 確認
3. このファイル（`docs/handoff-a-main-011-to-012-20260504.md`）を読了
4. `docs/dispatch-main-no38-*.md` を読み、a-bloom-002 応答待ち状態を把握
5. 東海林さんに「a-main-012 起動完了。a-bloom-002 の応答受領 or 次タスクの指示お願いします」と返答
