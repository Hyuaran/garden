# Codex-322 リストマスタ「分析」：表の見出しで昇順⇄降順／①は購入先を複数選んで絞る／①の表が横スクロールしないように

作成日: 2026-09-13
作業ツリー: C:\garden\a-bloom-008
起点: main 393ef89（`git log -1` で一致を確認してから着手）
**git は触らないこと（commit するな）。本番のデータに触らないこと。開発サーバ・ブラウザを起動しないこと。DB・API は今回触らない（画面だけ）。**

必ず先に読むもの：
- 画面：src/app/system/list/_components/ListMasterClient.tsx（`renderAnalysisBlock`・`analysisSelections`・`showAllSegments`・`filterActiveSegments`＝15 日側の合計の作り直し・リストタブの一覧の並べ替え `SEARCH_TABLE_COLUMNS` と見出しの ▲▼）・list-master.module.css（`.analysisTable`・`.sortHeaderButton`・`.multiSelect*`）・MultiSelectFilter.tsx（絞り込みで使っている複数選択の部品。`groups`＝{ label?, options: SoilListOptionItem[] }）
- ④正本：C:\Claude\000_Garden\150_System_システム\00_マニュアル\14_リストマスタ\Garden_System_リストマスタ_4_仕様書_Claude読み込み用.md §8-7（分析の作り）

## 0. 何を直すか（東海林さん 2026-09-13）

```
┌ ① どこから購入したか ？          購入先で絞る [ データ総研、ラディッシュ ほか1（3） ▼ ]  ┐
│ ( 円グラフ )                                                                        │
│ 購入先 ▲    │件数 ▼│コール済み│総コール回数│回転│有効│受注（案件）│獲得（コール）│受注率（有効）│受注率（総数）│
│ 合計（選んだ 3 つ） │ …  ← 選んだ購入先だけの合計                                              │
│ データ総研          │ …                                                                       │
│ ラディッシュ        │ …                                                                       │
│ 日本データ総研株式会社│ …   ← 長い名前は「…」で切って、乗せると全文                                  │
└──────────────────────────────────────────────────────────────┘
   ・見出しを押すと昇順⇄降順（同じ列をもう一度押すと反転。▲▼ は SVG 線画・リストタブの一覧と同じ見た目）
   ・②③も同じく見出しで並べ替えできる
   ・表は枠の幅に収まる（横スクロールを出さない）。①だけ横スクロールが出ていたのは購入先の名前が長いため
```

## 1. 見出しで昇順⇄降順（①②③すべて）
- 列＝区切り（購入先／リスト名／既契約情報）・件数・コール済み・総コール回数・回転・有効・受注（案件）・獲得（コール）・受注率（有効）・受注率（総数）の 10 列すべて
- 「合計」の行は**常に一番上**に固定（並べ替えの対象外）
- 既定は今のまま（件数が多い順）。並べ替えの状態はブロックごとに持つ（①で変えても②は変わらない）。「すべて表示する」の前後で並びは保つ
- 見出しの見た目・▲▼・押せること（`button`）はリストタブの一覧（`sortHeaderButton`）と**同じ部品／同じ CSS を使う**（コピーして別物にしない）
- 文字の列（区切り）は `localeCompare(…, "ja-JP")`。同じ値のときは区切り名で安定させる

## 2. ①購入先で絞る（複数選択）
- ①の見出し行の右（②の「直近 30 日｜15 日」と同じ位置）に **MultiSelectFilter**（絞り込みで使っている部品）を置く。ラベル「購入先で絞る」。選択肢＝①の区切り（合計を除く全部・件数の多い順・`count`＝件数）。**2,400 種類あるので、パネルの上に「名前で絞る」の入力欄を足す**（入力した文字を含むものだけ並べる。MultiSelectFilter に `searchable?: boolean` のような任意の props を足してよい。絞り込みの 4 項目の見た目・動きは変えない）
- 何も選んでいないとき＝今のまま（全部）
- 選んだとき＝表は選んだ購入先だけ（上位 50 行の制限は選んだものには掛けない）。**「合計」の行は選んだ購入先だけで作り直す**（`filterActiveSegments` の 15 日側と同じやり方：件数・コール済み・総コール回数・無効・受注・獲得を足し、有効・回転・受注率を計算し直し、円グラフの内訳も足し直す）。行の名前は「合計（選んだ 3 つ）」
- 円グラフは選択中の行（既定＝作り直した合計）
- 選択は画面を離れると消えてよい（保存しない）

## 3. ①の表が横スクロールしないように
- 区切りの列（購入先など）の文字を `<span>` で包み、**最大 200px・はみ出しは「…」・乗せると全文（title）**。表全体は枠の幅に収める（`.analysisTable` の `min-width: 960px` を外し、数字の列は `white-space: nowrap` のまま）。②③も同じ作り
- 直したあと、幅 1,000px 前後の枠で①②③とも横スクロールが出ないこと（数字 9 列＋名前 200px で約 950px）

## 4. テスト
- 並べ替え：見出しを押すと asc → もう一度で desc、合計は先頭のまま、別の列を押すとその列の asc
- 購入先で絞る：2 つ選ぶと表がその 2 行＋「合計（選んだ 2 つ）」になり、合計の件数が 2 行の和になる／外すと元に戻る／名前で絞る入力で選択肢が減る
- 既存テスト（src/app/system/list）は全部通す

## 5. 変えてはいけないもの
- 集計の中身・API・DB（画面の並べ替えと絞り込みだけ）
- 「？」・凡例・結果ボタン・一覧（detail）の動き

## 6. 受け入れ基準
1. vitest（src/app/system/list）が通る　2. tsc に今回の変更由来のエラーが無い　3. eslint（変更ファイル単位）が通る
4. Claude が本番で：①②③の見出しで並べ替え／①で購入先を 2 つ選んで合計が作り直される／①②③とも横スクロールが無い、を実測

## 7. 完了報告（コードブロックで出力）
- 「未コミット」と明記／変更・追加ファイルのフルパス／受け入れ基準 1〜3 の結果／本番データ・git に触っていないことの明記

## 8. 既知の差分（Codex が追記する）
