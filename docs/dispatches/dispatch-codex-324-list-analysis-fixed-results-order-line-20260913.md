# Codex-324 リストマスタ「分析」：円グラフの結果の並びを固定（前確OK・獲得は必ず出す）／凡例の下に「うち受注（案件）n 件」の行

作成日: 2026-09-13
作業ツリー: C:\garden\a-bloom-008
起点: main 1eb925f（`git log -1` で一致を確認してから着手）
**git は触らないこと（commit するな）。本番のデータに触らないこと。開発サーバ・ブラウザを起動しないこと。DB（集計表・migration）は触らない。触るのは API の「まとめ方」（analysis.ts）と画面だけ。**

必ず先に読むもの：
- API のまとめ方：C:\garden\a-bloom-008\src\app\api\soil\list\analysis\_lib\analysis.ts（`TOP_RESULT_LIMIT`・`RESULT_ORDER`・`finalizeSegment`・`loadAnalysisDetail` の「その他」の判定）とそのテスト C:\garden\a-bloom-008\src\app\api\soil\list\analysis\route.test.ts
- 画面：C:\garden\a-bloom-008\src\app\system\list\_components\ListMasterClient.tsx（`renderAnalysisBlock` の `analysisLegend`／`resultColor`／購入先で絞ったときと直近 15 日の「合計の作り直し」＝`results` を足し直しているところ（560〜590 行あたり）／`ANALYSIS_HELP` の円グラフの説明）・list-master.module.css（`.analysisLegend*`）・ListMasterClient.test.tsx
- ④正本：C:\Claude\000_Garden\150_System_システム\00_マニュアル\14_リストマスタ\Garden_System_リストマスタ_4_仕様書_Claude読み込み用.md §8-7

## 0. 何を直すか（東海林さん 2026-09-13 20:3x）

本番で見つかったこと（購入先＝株式会社Luna・受注 332 件）：
- 表では受注（案件）332 なのに、円グラフに受注が見えない。円グラフは「コール履歴の最終結果」の内訳で、受注（案件）は Kintone の案件から数えたもの＝別の数え方。332 件は 前確OK 131／担不 74／留守 57／NG 56／未コール 7／（結果なし）4／見込 2／無効 1 に散っている
- さらに、**Luna の凡例に「前確OK」が無い**。円グラフが「件数の多い 6 つ（未コール以外）＋その他」で作られているため、（結果なし）783 件のほうが多い購入先では 前確OK 131 件が「その他 141 件」（前確OK 131＋トス 6＋前確NG 4）に隠れる

直したあと：

```
      ( 円グラフ )        ● 留守            44,562 件
                          ● 担不            17,463 件
                          ● 無効            12,456 件
                          ● NG                 447 件
                          ● 前確OK             131 件   ← 必ず出す（0 件でも出す）
                          ● 見込               346 件
                          ● 獲得                 0 件   ← 必ず出す（0 件でも出す）
                          ● 未コール       118,626 件
                          ● その他              10 件   ← 上の 9 つ以外（トス・前確NG など）
                          ● （結果なし）       783 件
                          ─────────────────────────────
                            うち受注（案件）    332 件   ← 押せない・Kintone の案件から数えた数
                            コール結果とは別の数え方です
```

## 1. 円グラフの結果を「固定の並び」にする（API・analysis.ts）
- `TOP_RESULT_LIMIT`（上位 6 つ）の仕組みをやめ、**固定のリスト** `FIXED_RESULTS = ["留守", "担不", "無効", "NG", "前確OK", "見込", "獲得", "未コール", "（結果なし）"]` に入るものはそのまま、それ以外（トス・前確NG・その他の表記など）を「その他」にまとめる。`finalizeSegment` と `loadAnalysisDetail`（その他を押したときの一覧）の両方を同じ判定にする（判定は 1 つの関数にして両方から呼ぶ）
- 凡例の並び＝`RESULT_ORDER = ["留守", "担不", "無効", "NG", "前確OK", "見込", "獲得", "未コール", "その他", "（結果なし）"]`（その他は未コールの後・（結果なし）は最後）
- **前確OK と 獲得は 0 件でも `results` に入れる**（`rowCount: 0`）。ほかは件数があるものだけ（今と同じ）
- 「（結果なし）」の表記は集計表の値そのまま（`result` 列に入っている文字列）。表記ゆれ（坦不→担不など）のまとめ方は変えない
- route.test.ts：上位 6 つの前提になっているテストがあれば固定リストの前提に直す。足すテスト＝「前確OK が少なくても その他 に入らない」「トス・前確NG は その他 に入る」「獲得 0 件でも results に出る」「detail の その他 は固定リスト以外だけ」

## 2. 画面（ListMasterClient.tsx）
- `resultColor` に 獲得・（結果なし）・その他 の色が無ければ足す（獲得＝ティール系の濃い色・（結果なし）＝薄い灰色。既存の色とかぶらないこと）。0 件の切れは円グラフに描かれなくてよい（chart.js は 0 を描かない）が、凡例には出す
- 購入先で絞ったとき・直近 15 日の「合計の作り直し」で `results` を足し直している箇所は、並びを `RESULT_ORDER` と同じにする（今は「合計」の並びを借りている。固定の並びの定数を画面側にも 1 つ持ってよい）
- **凡例の下に「うち受注（案件）n 件」の行**を足す：凡例の最後に区切り線（`border-top: 1px solid var(--line)`）を引き、`● 印なし`・左「うち受注（案件）」・右に `formatCount(selected.orderCount) 件`（件数は凡例の件数と同じ列に右ぞろえ・`tabular-nums`）。**押せない**（`button` にしない・`div`）。その下に小さく（12px・`var(--sub)`）「Kintone の案件から数えた数。コール結果とは別の数え方です」
- `ANALYSIS_HELP` の円グラフの説明を直す：「円グラフ＝コール履歴の最終結果。留守／担不／無効／NG／前確OK／見込／獲得／未コール／（結果なし）を固定で出し、それ以外は『その他』。受注（案件）は Kintone の案件から数えたもので、円グラフとは別の数え方」の趣旨
- ListMasterClient.test.tsx：足す＝「うち受注（案件）」の行が出る・button でない／凡例に 前確OK が 0 件でも出る

## 3. 変えてはいけないもの
- 集計表・migration・cron・refresh
- 凡例の行を押すと一覧が開く動き（Codex-323）・表の並べ替え・購入先で絞る・見出し 2 段

## 4. 受け入れ基準
1. vitest（src/app/system/list と src/app/api/soil/list/analysis）が通る　2. tsc に今回の変更由来のエラーが無い　3. eslint（変更ファイル単位）が通る
4. Claude が本番で：Luna の凡例に 前確OK 131 件・獲得 0 件が出る／その他が 10 件（トス 6＋前確NG 4）になる／「うち受注（案件）332 件」の行が出て押せない／合計（全体）の凡例も固定の並びになる、を実測

## 5. 完了報告（コードブロックで出力）
- 「未コミット」と明記／変更・追加ファイルのフルパス／受け入れ基準 1〜3 の結果（実行したコマンドと結果）／本番データ・git に触っていないことの明記

## 6. 既知の差分（Codex が追記する）
