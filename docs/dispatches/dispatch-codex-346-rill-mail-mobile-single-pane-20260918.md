# Codex-346 Rill：スマホ・タブレット幅でメール画面を 1 列ずつ切り替える（箱 → 一覧 → 本文・［戻る］）＋ツールバーは折り返し

作成日: 2026-09-18
作業ツリー: C:\garden\a-bloom-008
起点: main の最新（`git log -1` で確認・完了報告に書く）
**git は触らないこと（commit するな）。本番のデータ・DB・Microsoft のメールに触らないこと。開発サーバ・ブラウザを起動しないこと。migration は不要。触ってよいのは `src/app/rill/mail/_components/RillMailScreen.tsx`・`RillMailScreen.module.css`・`RillComposePane.tsx` とそのテストだけ。メールの読み書き・検索・ピン・状態・取り込み・下書き・送信取り消しの動き（`_lib/`・API）は変えない。**

必ず先に読むもの：
- 見た目の正本：C:\Users\shoji\iCloudDrive\iCloud~md~obsidian\Knowledge\070_AI-Rules\050_Gardenスタイル・社長スタイル.md（§3 の 7〜10）
- 今のコード：`RillMailScreen.tsx` の `styles.panes`（3 列＝箱の一覧 `aside.column`・メール一覧 `section.column`・本文）とツールバー `styles.toolbar`、`RillMailScreen.module.css` の `.panes{grid-template-columns:206px minmax(0,1.02fr) minmax(0,1.2fr)}`・`.toolbar`

## 0. 何を直すか（Claude の本番実機確認・2026-09-18 23:5x）

幅 390px で /rill/mail を開くと、3 列のまま詰め込まれて、メール一覧は右が切れ・本文の列は画面外・ツールバーには横スクロールのつまみが出る。枠（上の帯・引き出し）は Codex-345 で整っているので、**メール画面の中身だけ**を狭い幅向けにする。

## 1. 画面（幅 760px 以下）

```
① 一覧（最初に出る）                 ② メールを押したとき               ③ 箱を選ぶとき
┌──────────────────┐        ┌──────────────────┐     ┌──────────────────┐
│[更新][異常チェック][新規]…│        │[← 一覧へ]  [返信][転送]…│     │[← 一覧へ]            │
│[すべての箱 ▼]  152 件    │        │ 件名（2 行まで）          │     │ まとめて見る          │
│┌────────────────┐│        │ 差出人・日時             │     │  ● すべての箱         │
││ kanri@f…   shoji 9/18 ││        │ ─────────────── │     │  ● ピン止め           │
││ 取次進捗報告【株式会社…││        │ 本文（全幅）              │     │ 自分                 │
││ 株式会社ARATA ご担当者…││        │                        │     │  ● 東海林 美琴        │
│├────────────────┤│        │                        │     │ 共有                 │
││ …                     ││        │                        │     │  ● keiri …           │
│└────────────────┘│        └──────────────────┘     └──────────────────┘
└──────────────────┘
```

- **760px 以下は 1 列だけを見せる**：状態 `mobilePane: "list" | "detail" | "boxes"`（初期 "list"）。`.panes` を 1 列にし、今の 3 つの列のうち `mobilePane` に当たる 1 つだけ表示（他は `display:none`。**DOM から消さない**＝選択・スクロール位置・読み込みの状態を保つ）
- 一覧の上に **［すべての箱 ▼］ボタン**（今選んでいる箱の名前＋件数）→ 押すと "boxes"。箱を選ぶと "list" に戻る
- 一覧でメールを選ぶと "detail"。本文の上に **［← 一覧へ］**（線画の矢印＋文字）→ "list"。今の選択はそのまま
- 761px 以上は今のまま（3 列）。幅が切り替わったら "list" に戻す必要はない（3 列ではすべて見える）
- **ツールバー**：760px 以下は横スクロールをやめて**折り返し**（`flex-wrap:wrap`・行間 6px）。検索欄は 2 行目に全幅。ボタンの文字は折り返さない（nowrap）
- 一覧の 1 行：差出人は 1 行で省略（…）、件名は 2 行まで（`-webkit-line-clamp:2`）、本文の冒頭は 1 行で省略。日時・箱の札は右上に。**横にはみ出さない**
- 下書きの小窓・送信取り消しの帯・各種の小窓（異常チェック・添付・取り込み）は、幅 390px で画面内に収まる（最大 `calc(100vw - 24px)`）
- 文言は［← 一覧へ］と箱ボタンの「すべての箱」（今の箱名を出す）以外は足さない。絵文字・記号の矢印（←→）は使わず線画の SVG

## 2. 合格の物差し（完了報告に書く）
- 幅 390px・768px で、ページ全体の横スクロール 0・ツールバーの横スクロール 0・一覧の行のはみ出し 0
- 1866px・1280px は今と同じ 3 列

## 3. テスト（vitest・全部緑にしてから完了報告）
- 760px 以下の想定（`matchMedia` をモック）で：最初は一覧だけ見える／メールを押すと本文だけ・［← 一覧へ］で一覧へ／箱ボタンで箱の一覧・選ぶと一覧へ（選んだ箱で絞られる）
- 761px 以上の想定で 3 列が全部見える
- 既存の Rill のテストが全部通る
- `npx tsc --noEmit`・`npx vitest run src/app/rill`

## 4. 完了報告（コードブロックで）

```
Codex-346 完了報告
- 起点の main（git log -1）
- 触ったファイル
- 切り替えの作り（状態・CSS・幅の判定）
- ツールバー・一覧の行・小窓の狭い幅での直し
- テスト結果
- Claude が確認する手順
- 気になった点
```
