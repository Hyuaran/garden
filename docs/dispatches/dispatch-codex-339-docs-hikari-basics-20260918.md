# Codex-339 資料：「光回線・通信の基礎（新入社員研修）」を読み物として追加（挿絵 8 枚つき）

作成日: 2026-09-18
作業ツリー: C:\garden\a-bloom-008
起点: main の最新（`git log -1` で確認・完了報告に書く）
**git は触らないこと（commit するな）。本番のデータ・DB・Storage に書き込まないこと（Storage は読むだけ）。開発サーバ・ブラウザを起動しないこと。migration は不要。**

必ず先に読むもの：
- ④ 資料：C:\Claude\000_Garden\150_System_システム\00_マニュアル\11_資料\Garden_System_資料_4_仕様書_Claude読み込み用.md（§2 画面構成・§3-5 Storage・§6-1・§8「資料が増えるなら documents 配列に寄せる」）
- 原稿（本文の正）：C:\Claude\000_Garden\150_System_システム\06_光回線研修マニュアル\001_光回線_新入社員研修マニュアル_原稿.md
- 挿絵の一覧（どの章にどの絵か）：C:\Claude\000_Garden\150_System_システム\06_光回線研修マニュアル\002_ChatGPT指示_挿絵8枚.md（§【1枚目】〜【8枚目】の見出しだけ読めばよい）
- 今のコード：`src/app/system/docs/page.tsx`（一覧）・`_components/CompanyDocument.tsx`（読み物の作り＝これに倣う）・`_data/company-doc.ts`・`_lib/company-doc.server.ts`（`requireDocsUser`・署名 URL の作り方）・`docs.module.css`・`company/page.tsx`・テスト `_components/CompanyDocument.test.tsx`

## 0. 何を作るか（東海林さん 2026-09-17〜18）

System の「資料」画面に、会社説明と同じ読み物形式で **「光回線・通信の基礎（新入社員研修）」** を 1 本足す。本文は原稿の 7 章をそのまま（言い換え・要約・削除をしない。表・用語集も全部）。章ごとに挿絵（8 枚・Storage に置いてある）を入れる。

## 1. 画面

一覧（`/system/docs`）：会社説明の右に新しいカードを 1 枚。**JSX 直書きではなく `documents` 配列に 1 件足す**（title／description／updatedAt／href）。

```
資料
┌──────────────┐┌──────────────┐┌──────────────┐┌──────────────┐
│ 会社説明      ││ 光回線・通信の ││ 動画          ││ スライド       │
│              ││ 基礎（新入社員 ││              ││              │
│ 最終更新日 …  ││ 研修）        ││              ││              │
│ 資料を読む →  ││ 最終更新日 …  ││ 動画を見る → ││ スライドを見る→│
└──────────────┘└──────────────┘└──────────────┘└──────────────┘
```

読み物（`/system/docs/hikari-basics`）：会社説明と同じ `readingLayout`（左に目次・右に本文）。ヒーローは「光回線・通信の基礎」＋一言「新入社員向け 通信の基礎知識」＋最終更新日。**「オリエンテーション表示」ボタンは付けない**（投影は不要）。

```
System ／ 資料 ／ 光回線・通信の基礎（新入社員研修）
光回線・通信の基礎（新入社員研修）                最終更新日 2026年9月18日
┌─目次─────────┐ ┌──────────────────────────────────────────┐
│ 1 接続の基本構造  │ │ 1  インターネット接続の基本構造と関係性            │
│ 2 回線の歴史     │ │   本文（道路／料金所のたとえ・箇条書き 2 つ）         │
│ 3 速度の単位     │ │   [挿絵 1：道路と料金所]                          │
│ 4 NTT系と電力系  │ │   昔と今の契約形態の違い（箇条書き 2 つ）             │
│ 5 必須用語集     │ │   [挿絵 2：昔と今の契約]                          │
│ 6 電話番号の取扱い│ │ 2  通信回線の歴史（古い順）                        │
│ 7 やめるときの区分│ │   [挿絵 3：回線の歴史]                            │
└──────────────┘ │   表（順序／回線種別／仕組みと特徴／速度の目安／当時の特徴）│
                 │ 3  通信速度の単位と読み方                          │
                 │   本文＋箇条書き 5 つ                              │
                 │   [挿絵 4：速度の階段]                             │
                 │ 4  回線系統の違い（NTT系と電力系）                   │
                 │   箇条書き 2 つ  [挿絵 5：NTT系と電力系]              │
                 │ 5  新入社員向け 必須用語集                          │
                 │   [挿絵 6：家の中の機器のつながり]                    │
                 │   小見出し「機器・配線まわり」→ 用語 9 つ（用語：説明）   │
                 │   小見出し「工事・制度」→ 用語 3 つ                    │
                 │   [挿絵 7：有派遣と無派遣]（用語「有派遣」「無派遣」の直後）│
                 │ 6  電話番号の取り扱い（発番・乗り換え）                │
                 │   本文＋箇条書き 2 つ（注意点つき）                    │
                 │ 7  回線をやめるときの呼び方と区分                    │
                 │   本文＋表（呼び方／内容・目的／手続きのポイント／電話番号の行方）│
                 │   [挿絵 8：やめるときの 4 区分]                      │
                 │   目次へ戻る ↑                                    │
                 └──────────────────────────────────────────┘
```

- 挿絵は `figure`（画像＋`figcaption`＝短い題。例「回線＝道路、プロバイダー＝料金所」）。幅は本文いっぱい・角丸・薄い枠線。クリックで拡大は不要
- 表は 2 か所（§2・§7）。**スマホ幅（375px）で横スクロールを表の中だけに閉じ込める**（ページ全体は横スクロールしない）
- 用語集は `dl`（用語＝`dt`・説明＝`dd`）。原稿の**太字・注記（※〜）・「8日以内」等の強調**は `strong` で残す
- 見出しの番号つき章見出しは `chapterHeading` を流用。色・フォントは docs.module.css の既存の変数（社長スタイル）。絵文字は使わない。画面の文言に開発者用語を出さない

## 2. 作り（会社説明に倣う）

- `_data/hikari-basics.ts`（`import "server-only"`）：`hikariBasicsDocument`（title／description／updatedAt="2026-09-18"／eyebrow 等）・`hikariChapters`（7 章・id と番号と題）・本文のデータ（章ごとに 段落／箇条書き／表／用語／挿絵の並び）。**本文の文字列は原稿からそのまま**。挿絵は `{ id: "01", title: "…", alt: "…" }` の配列（8 件）
- `_lib/hikari-basics.server.ts`：`loadHikariBasicsFigures()`＝`requireDocsUser("/system/docs/hikari-basics")` のあと、8 枚ぶん `getSupabaseAdmin().storage.from("system-docs").createSignedUrl("docs/hikari-basics/<id>.webp", 3600)`。パスは承認済みデータ（配列の id）からだけ組み立てる。署名できなかった絵は**その figure を出さない**（本文は出す）。会社説明の写真と同じ方針
- `_components/HikariBasicsDocument.tsx`：`CompanyDocument` と同じ骨組み（パンくず・ヒーロー・目次・本文・目次へ戻る）。`img` は `referrerPolicy="no-referrer"`・`loading="lazy"`・eslint の no-img-element を会社説明と同じ書き方で回避
- `hikari-basics/page.tsx`：`metadata.title = "光回線・通信の基礎 | Garden"`。`loadHikariBasicsFigures()` → `HikariBasicsDocument`
- `page.tsx`（一覧）：`documents` 配列に足す（会社説明の次）。カードの説明文は「新入社員向けに、回線・プロバイダー・機器・工事・電話番号・解約の区分をまとめています。」
- `docs.module.css`：figure・表・用語集の分だけ足す（既存クラスは変えない）。ダークモードは既存の変数に乗る
- Storage の画像は Claude が置き済み：`system-docs/docs/hikari-basics/01.webp`〜`08.webp`（1448×1086・4:3・40〜70KB）

## 3. テスト（vitest・全部緑にしてから完了報告）
- `_components/HikariBasicsDocument.test.tsx`：7 章の見出しが番号つきで出る／目次のリンクが 7 本で章 id と一致／原稿の要所（「道路」「料金所」「8日以内」「事業者変更」「アナログ戻し」）が本文に出る／figures を 8 件渡すと img が 8 つ・alt がある／figures が空なら img が 0 で本文は出る
- `page.tsx` の一覧に新カードが出る（既存の page.test があれば足す）
- `npx tsc --noEmit`・`npx vitest run src/app/system/docs`

## 4. 完了報告（コードブロックで）

```
Codex-339 完了報告
- 起点の main（git log -1）
- 触ったファイル（新規／変更）
- 本文の入れ方（原稿とどう対応させたか・落とした文がないか）
- 挿絵の入れ方（署名 URL の作り・失敗時）
- テスト結果（tsc／vitest の件数）
- Claude が確認する手順（一覧のカード → 読み物 → 挿絵 8 枚 → スマホ幅で横スクロール無し）
- 気になった点
```
