# Codex-345 社長スタイルの枠（System／Root／Rill）：スマホ・タブレット幅で左メニューを「引き出し式」に＋Root の見出し・ボタン列を狭い幅でも整える

作成日: 2026-09-18
作業ツリー: C:\garden\a-bloom-008
起点: main の最新（`git log -1` で確認・完了報告に書く）
**git は触らないこと（commit するな）。本番のデータ・DB に触らないこと。開発サーバ・ブラウザを起動しないこと。migration は不要。触ってよいのは下の「触るファイル」とそのテストだけ。**

必ず先に読むもの：
- 見た目の正本：C:\Users\shoji\iCloudDrive\iCloud~md~obsidian\Knowledge\070_AI-Rules\050_Gardenスタイル・社長スタイル.md
- 今の枠：`src/app/system/_components/ShachoShell/ShachoShell.tsx`・`shacho-shell.module.css`・`SidebarNavigation.tsx`／`src/app/root/_components/RootShell.tsx`・`root-shell.module.css`（Root は System の CSS を import して共有）／`src/app/rill/_components/RillShachoShell.tsx`・`RillShachoShell.module.css`
- Root の見出し：`src/app/root/_components/PageHeader.tsx`・`Button.tsx`

触るファイル：上の 3 つの枠の tsx／css、`src/app/root/_components/PageHeader.tsx`、`src/app/root/employees/page.tsx`（見出し右のツール列だけ）、`src/app/root/bank-check/page.tsx`（絞り込みボタン列だけ）、`src/app/root/page.tsx`（説明文だけ）とそれぞれのテスト。

## 0. 何を直すか（Claude の本番実機確認・2026-09-18 23:3x）

幅 390px（スマホ）で Root を開くと、左メニュー（168px）が残って本文が約 200px の帯になり、表は 2 列しか見えず、「すべての法人」の選択欄が縦に伸び、「最終同期 なし」が 1 文字ずつ縦に潰れる。System と Rill も同じ枠なので同じ。**販売品質**（今回の合格基準）にするため、狭い幅では左メニューを隠して引き出し式にする。

## 1. 画面（幅 760px 以下）

```
┌──────────────────────────────┐
│ [≡]  [樹] Garden ／ Root         [☀][⇥]│ ← 上の帯（高さ 56px・カード地・下に罫線・上に固定）
├──────────────────────────────┤
│ 従業員マスタ                        │
│ 給与処理対象者。退職時は…              │
│ [すべての法人          ▼]           │ ← 見出しの右のツールは見出しの下に全幅で並ぶ
│ [氏名・番号で検索（Ctrl+Shift+G）]    │
│ [名簿と同期] 最終同期 なし             │
│ [Chatwork トークンを取り込む][＋ 新規追加]│
│ ┌──────────────────────────┐ │
│ │ ID  社員番号  氏名 …  →（表の中だけ横スクロール）│ │
│ └──────────────────────────┘ │
└──────────────────────────────┘

[≡] を押すと左から引き出す：
┌─────────────────┬────────────┐
│ [樹] Garden        [×]│ ░░░（暗幕）░░ │
│ Root ／ 組織台帳       │ ░░░░░░░░░░ │
│ ┌─モジュール──────┐│ ░░░░░░░░░░ │
│ │[S][B][F][S]…[R]  ││ ░░░░░░░░░░ │ ← 左端のアイコン列は、引き出しの中で横に並べる（折り返し可）
│ └────────────┘│ ░░░░░░░░░░ │
│ メニュー              │ ░░░░░░░░░░ │
│ ▢ ホーム              │ ░░░░░░░░░░ │
│ ■ 従業員マスタ         │ ░░░░░░░░░░ │
│ …                   │ ░░░░░░░░░░ │
│ (東) 東海林 美琴       │ ░░░░░░░░░░ │
│     株式会社ヒュアラン  │ ░░░░░░░░░░ │
│     全権管理者        │ ░░░░░░░░░░ │
└─────────────────┴────────────┘
 幅 280px（画面の 85% まで）
```

- **760px 以下**：左端のアイコン列（rail）と左メニュー（side）を隠し、**上の帯**を出す。帯＝左に［≡］（線画・aria-label「メニューを開く」）、その右に樹のマーク＋「Garden ／ Root」（System は「Garden ／ System」、Rill は「Garden ／ Rill」）、右端に今の右上の操作（テーマ切替・ログアウト。Rill は今の上の帯の操作をそのまま・入りきらない物は利用者メニューの中へ）
- ［≡］で**引き出し**（左から・幅 280px・最大 85vw・`transform` で 0.18s）＋暗幕。中身＝今の左メニューと同じ（ブランド・メニュー・利用者欄）に、**モジュールのアイコン列を横並び**で足す（System を先頭に・今いるモジュールが選択中）
- 閉じる：［×］・暗幕を押す・Esc・メニューの項目を押して移動したとき。開いている間は本文をスクロールさせない（`body` に overflow hidden）。フォーカスは引き出しの中に（開いたら［×］へ・閉じたら［≡］へ戻す）。`aria-expanded`・`aria-controls`・`role="dialog"`＋`aria-modal`
- **761px 以上は今と同じ**（rail＋side を常に表示）。今の 800px・640px の段階の縮み方はそのまま残してよいが、760px 以下は引き出し式が優先
- 本文の余白：760px 以下は `16px 16px 40px`（今の右上の操作の絶対配置は帯へ移るので、本文上の 54px の空きは要らない）
- サイドバー無しの役職（`shouldHideSidebar`）は今のまま（帯も出さない）

## 2. Root の見出しとボタン列（狭い幅）

- `PageHeader`：760px 以下は題と説明が上、ツール（actions）はその下に**全幅で折り返し**。select・input は `flex: 1 1 100%`（最低 0）、ボタンは内容幅（nowrap）
- 従業員マスタ：「最終同期 なし」の小さな文字が 1 文字ずつ縦に潰れている → この文字は `white-space: nowrap` に
- 口座の点検：「指摘で絞る：すべて／コード無し／台帳に無い／廃止済み／名前が違う／支店名なし」のボタン列が細い箱に押し込まれて潰れる → 列は `flex-wrap: wrap`・各ボタン nowrap
- Root ホームの説明文「Garden シリーズ全モジュールで参照される基礎マスタ。削除は不可、無効化で管理します。」が狭い幅で行頭に「、」「。」が来る → 枠に入れた `word-break: auto-phrase; line-break: strict; text-wrap: pretty`（main eaff2f8）で直るはず。直らなければ文の区切りを変えずに、その `p` に `text-wrap: pretty`

## 3. 合格の物差し（完了報告に結果を書く）
- 幅 390px・768px・1280px・1866px の 4 段階で、**ページ全体の横スクロール 0**、表は表の中だけ横スクロール、**1〜2 文字ずつの縦潰れ 0**、ボタンの文字の折り返し 0、ボタン同士の重なり 0
- 引き出しの開閉がキーボードだけでもできる

## 4. テスト（vitest・全部緑にしてから完了報告）
- 3 つの枠それぞれ：［≡］で開く（`aria-expanded` が true・引き出しが見える）／［×］・暗幕・Esc・項目クリックで閉じる／引き出しの中にモジュールのアイコン列（System が先頭・今のモジュールが選択中）とメニューと利用者欄がある
- 既存の System／Root／Rill のテストが全部通る
- `npx tsc --noEmit`・`npx vitest run src/app/system src/app/root src/app/rill`

## 5. 完了報告（コードブロックで）

```
Codex-345 完了報告
- 起点の main（git log -1）
- 触ったファイル
- 引き出しの作り（3 つの枠で共通化したか・キーボード・aria）
- 狭い幅での Root の見出し・ボタン列の直し
- テスト結果
- Claude が確認する手順（幅 390／768 で開閉・各画面）
- 気になった点
```
