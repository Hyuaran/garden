# Codex-340 リストマスタ：リストタブの絞り込みの並び替え（3 段に区切る・から／まで の表示・「購入履歴」を外す）

作成日: 2026-09-18
作業ツリー: C:\garden\a-bloom-008
起点: main の最新（`git log -1` で確認・完了報告に書く）
**git は触らないこと（commit するな）。本番のデータ・DB に触らないこと。開発サーバ・ブラウザを起動しないこと。migration は不要。触ってよいのは `src/app/system/list/_components/ListMasterClient.tsx`（リストタブの絞り込みフォーム `filterGrid` の範囲と、その周辺の関数）・`list-master.module.css`・`src/app/system/list` 配下のテストだけ。API・条件 JSON の形（`_lib/list-fields.ts`・`src/app/api/soil/list`）は変えない。**

必ず先に読むもの：
- ④ リストマスタ：C:\Claude\000_Garden\150_System_システム\00_マニュアル\14_リストマスタ\Garden_System_リストマスタ_4_仕様書_Claude読み込み用.md（§2-1 リスト・§4-1 条件（JSON）・§8-8）
- 今のコード：`ListMasterClient.tsx` の `filterGrid` の JSX（`<form onSubmit={handleFilterSubmit}>` の中）・`filtersToConditions`／保存条件を画面に戻す関数（`purchaseHistoryExists` を扱っている箇所）・`describeFilters`（条件の要約文）・`list-master.module.css` の `.filterGrid` `.wide` `.range` `.searchCell` とスマホ幅（`@media` の 1 列化）

## 0. 何を変えるか（東海林さん 2026-09-18）

1. **並び順を 3 段に分けて置き直す**（下の絵のとおり）。段の間に薄い区切り線
2. **範囲入力に「から／まで」「回以上／回以下」の表示**を付ける（経過（年）の「年以上／年以下」と同じ見え方に揃える）
3. **「購入履歴」の絞り込みを画面から外す**（台帳の「購入履歴あり」は最新購入先が入っているかと完全に一致＝あり 1,668,513／なし 1,007,024 で、購入先の「（空欄）」と同じことができるため）。**条件 JSON・API・保存済みの条件は壊さない**（下の §2）

## 1. 画面（リストタブ「絞り込み」・4 列グリッドのまま）

```
絞り込み                                                        [条件を保存]

 都道府県          購入先            区分              購入状態
 [指定なし    ▼]  [指定なし    ▼]  [指定なし    ▼]  [指定なし    ▼]
 元回線           経過（年）
 [指定なし    ▼]  [年以上      ][年以下      ]
 ─────────────────────────────────────────────────────────────
 リスト投入日                        リスト名
   から              まで
 [年/月/日   ▢][年/月/日   ▢]      [含む                          ]
 再判定日                            最終コール日
   から              まで              から              まで
 [年/月/日   ▢][年/月/日   ▢]      [年/月/日   ▢][年/月/日   ▢]
 コール回数
   回以上            回以下
 [           ][           ]
 ─────────────────────────────────────────────────────────────
 自社アポ禁        アポ禁            AU光架電可否              
 [なし        ▼]  [（空欄）    ▼]  [○          ▼]      [  検索  ]
```

- 1 段目＝**番号の属性**（都道府県／購入先／区分／購入状態／元回線／経過（年））
- 2 段目＝**リストとコールの日付・回数**（リスト投入日／リスト名／再判定日／最終コール日／コール回数）
- 3 段目＝**架電してよいか**（自社アポ禁／アポ禁／AU光架電可否）＋［検索］（右端・下ぞろえ）
- 区切り線＝グリッドの 1 行を丸ごと使う薄い横線（`grid-column: 1 / -1`・`border-top: 1px solid var(--line)`・上下 4px 程度の余白）。スマホ幅（1 列）でも出す
- 「から／まで」「回以上／回以下」「年以上／年以下」は**入力欄の上に小さく**（12px・薄い色）出す。日付欄は placeholder が効かないので、5 つの範囲項目すべてこの「上の小さな文字」で揃える（経過（年）の placeholder は残してよい）
- 幅：範囲入力は今と同じ 2 枠ぶん（`label:has(.range)`）。リスト名は `wide`（2 枠）。1 段目の 6 項目は 1 枠ずつ
- 選択肢・既定値・保存条件の名前・「条件を変える」に畳んだあとの要約文（`describeFilters`）の並びも、新しい並びに合わせる（要約文は「都道府県／購入先／区分／購入状態／元回線／経過（年）／リスト投入日／リスト名／再判定日／最終コール日／コール回数／自社アポ禁／アポ禁／AU光架電可否」の順）

## 2. 「購入履歴」の外し方（互換を保つ）

- フォームから `<select>` を消すだけ。`filters.purchaseHistory` の state・`filtersToConditions`（`purchaseHistoryExists` を作る）・保存条件を画面に戻す処理・`describeFilters` の「購入履歴：あり」は**残す**（昔に保存した条件に `purchaseHistoryExists` が入っていても、読み込めば効き、要約文にも出る）
- 新しく保存する条件には入らない（フォームに無いので）
- ④ §2-1・§4-1 の記述は Claude が直す（Codex は触らない）

## 3. テスト（vitest・全部緑にしてから完了報告）
- 絞り込みフォームのラベルが上の並び順で DOM に出る（都道府県 → 購入先 → 区分 → 購入状態 → 元回線 → 経過（年） → リスト投入日 → リスト名 → 再判定日 → 最終コール日 → コール回数 → 自社アポ禁 → アポ禁 → AU光架電可否）
- 「購入履歴」の select が無い／`purchaseHistoryExists` 入りの保存条件を読み込むと検索条件に効き、要約文に「購入履歴：あり」が出る
- 範囲項目 5 つに「から／まで」「回以上／回以下」「年以上／年以下」の文字が出る
- 区切り線が 2 本ある
- `npx tsc --noEmit`・`npx vitest run src/app/system/list`

## 4. 完了報告（コードブロックで）

```
Codex-340 完了報告
- 起点の main（git log -1）
- 触ったファイル（新規／変更）
- 並びと区切りの作り（グリッドのどこで区切ったか・スマホ幅）
- 「から／まで」の出し方
- 購入履歴の外し方（互換の残し方）
- テスト結果
- Claude が確認する手順
- 気になった点
```
