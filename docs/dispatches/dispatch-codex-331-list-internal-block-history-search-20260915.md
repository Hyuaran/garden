# Codex-331 リストマスタ：自社アポ禁履歴（1 件登録・ファイル一括・解除）＋ 履歴検索タブ（電話番号 1 つの時系列）

作成日: 2026-09-15
作業ツリー: C:\garden\a-bloom-008
起点: Codex-330 のマージ後の main（`git log -1` を控えてから着手）
**git は触らないこと（commit するな）。本番のデータ・DB・Kintone に触らないこと。開発サーバ・ブラウザを起動しないこと。migration は supabase/migrations に書くだけで実行しない（実行は Claude）。**

必ず先に読むもの：
- ④：C:\Claude\000_Garden\150_System_システム\00_マニュアル\14_リストマスタ\Garden_System_リストマスタ_4_仕様書_Claude読み込み用.md（**§2 画面構成・§2-4 管理方法・§3 データモデル（5 つの表の呼び方）・§4-2 書き出し・§4-3 コール履歴・§8-9 #6 #7 と決定 (c)・§9**）
- 台帳の `アポ禁`（購入元由来の値）と `クレーム履歴`・`AU光架電可否` の意味：C:\Claude\027_リストマスタクリーニング\新リストマスタ_フィールド定義書_v1.md。**決定 (c)：自社アポ禁は AU光架電可否を × にしない**（別の印として持つ）
- 画面：C:\garden\a-bloom-008\src\app\system\list\_components\ListMasterClient.tsx（4 タブ・管理方法タブ・絞り込み・書き出し）、`MultiSelectFilter.tsx`、`list-master.module.css`
- 書き出し：C:\garden\a-bloom-008\src\app\api\soil\list\_lib\export-sql.ts・export-format.ts・mer.ts、`src\lib\db\pg.ts`（`forEachPgBatch`）
- アップロードの解析（ファイル取込の部品）：C:\garden\a-bloom-008\src\app\api\soil\list\_lib\upload-parser.ts（`readRows`・`decodeText`・`normalizePhone`）
- 履歴の表：`soil_list_purchase`（購入履歴）・`soil_list_assignment`（投入履歴）・`soil_list_call`（コール履歴＝電話番号×リスト名の集約。FM 取込＋Garden 日次）・`system_call_history`（Garden の通話記録 1 件ずつ・2026-01〜・53 万件・列 call_date/call_time/employee_name/result_flag/note/list_name/phone_number）・`soil_list_order`（受注履歴）・`system_fm_shineigyo`（新営業の既契約）・台帳の `住ポン_取得日`

## 0. 何を作るか（東海林さん 2026-09-15・④ §8-9 #6 #7）

1. **自社アポ禁履歴**：重クレーム等で「ヒュアラン社内だけで、今後一切架電しない」番号の台帳。1 件ずつでも、CSV／Excel で一括でも登録でき、解除もできる。**AU光架電可否は変えない**（決定 c）。効かせ先＝リストタブの絞り込み「自社アポ禁」（既定＝なし）と、書き出し（自動で除く）
2. **履歴検索タブ**：電話番号 1 つを入れると、その番号の全ての履歴を 1 本の時系列で見られる

```
タブ：リスト｜アップロード｜分析｜履歴検索｜管理方法
```

## 1. データ（migration 1 本・`20260917000001_soil_list_internal_block.sql`）

- 新表 `soil_list_internal_block`（**自社アポ禁履歴**）：id uuid・電話番号 text（数字のみ）・登録日 date・理由 text・登録者 text（社員名）・出所 text（'画面'／'ファイル'＋ファイル名）・解除日 date null・解除者 text null・解除理由 text null・created_at・updated_at。索引（電話番号）。RLS 有効・ポリシーなし・service_role のみ（読み書きは API）
- 台帳 `soil_list_phone` に `自社アポ禁 boolean not null default false`（今の値＝解除されていない行があるか。登録／解除の API が更新する。索引）。**台帳に無い番号も登録できる**（その番号が後で投入されたら `soil_list_apply_upload` の新規 insert で `自社アポ禁` を履歴から引く）
- `soil_list_refresh_options()` は変えない（絞り込みは なし／あり の 2 択）

## 2. 管理方法タブに「自社アポ禁」の枠

```
┌ 自社アポ禁（ヒュアラン社内だけの架電禁止。購入元のアポ禁とは別）────────────┐
│ 1 件登録：電話番号 [__________]  理由 [________________________]  [ 登録 ]      │
│ ファイルで一括：[ CSV／Excel をここに ]  列＝電話番号・理由（1 行目は見出し）      │
│   → 中身の確認（n 件・重複 n・すでに登録済み n・番号の形が違う n）→ [ 登録する ]  │
│ 登録済み一覧（新しい順・100 件ずつ・番号で探す）                                  │
│   電話番号 ｜ 登録日 ｜ 理由 ｜ 登録者 ｜ 出所 ｜ [解除]                           │
│   解除した行は薄く（解除日・解除者）。[解除済みも見る] で表示                       │
└──────────────────────────────────────────────────────────────────────────┘
```

- 権限：登録・解除は **manager 以上**（画面と同じ）。理由は必須
- API：`GET /api/soil/list/internal-block?q=&page=&includeReleased=`／`POST`（1 件）／`POST /api/soil/list/internal-block/preview`（ファイル解析だけ）／`POST /api/soil/list/internal-block/bulk`（一括・1,000 件ずつ）／`PATCH /api/soil/list/internal-block/{id}`（解除＝解除日・解除者・解除理由）。同じ番号が未解除で既にあれば足さない（件数に出す）
- 登録・解除のたびに台帳の `自社アポ禁` を更新

## 3. 絞り込みと書き出し

- リストタブに絞り込み「自社アポ禁」（なし／あり／指定なし・**既定＝なし**＝一覧に出ない）。13 → 14 項目。条件の要約・保存条件に含める
- 書き出し（3 形式）は **条件に関係なく `自社アポ禁=true` を除く**。結果に「自社アポ禁 n 件を除きました」を出し、書き出し記録（`soil_list_export`）にも `excluded_internal_block` を残す
- 一覧の行に印（「自社アポ禁」の小さな赤いラベル）。分析タブの数字は変えない（集計対象のまま）

## 4. 履歴検索タブ

```
┌ 履歴検索 ─────────────────────────────────────────────────────────────┐
│ 電話番号 [0285720215]  [ 検索 ]                                          │
│ ─ 今の値（電話番号台帳）────────────────────────────────────────────── │
│  佐藤 民一 ｜ 栃木県芳賀郡益子町… ｜ リスト名 【光回線】アナログ_20260914 ｜ 元回線 アナログ │
│  AU光架電可否 ○ ｜ アポ禁 (空欄) ｜ 自社アポ禁 なし ｜ 購入状態 完パケ ｜ コール回数 3 ｜ 最終結果 留守 │
│ ─ 履歴（新しい順）──────────────────────────────────────────────────── │
│  2026-09-14  投入     【光回線】アナログ_20260914（アップロード 2026-09-15・東海林）   │
│  2026-09-14  購入     購入先 ○○（購入履歴）                                        │
│  2026-08-03  コール   留守 ｜ 担当 田中 ｜ リスト 【光回線】アナログ_20260803 ｜ メモ…    │
│  2026-08-01  コール   担不 ｜ …                                                    │
│  2026-07-31  コール（FM 取込の集約） コール回数 2・最終結果 NG（旧リスト名 …）          │
│  2026-05-10  受注     商材 ○○ ｜ チーム ○○ ｜ 営業ID ○○ ｜ キャンセル日 —             │
│  2026-04-01  既契約（新営業） 既契約情報 …                                           │
│  2026-09-10  住所でポン 取得（年版 2026）                                            │
│  2026-06-01  自社アポ禁 登録（理由：重クレーム・登録者：東海林）／解除                 │
└──────────────────────────────────────────────────────────────────────┘
```

- 種類ごとに色の帯（SVG 線画アイコン可・絵文字禁止）。種類で絞るチェック（投入／購入／コール／受注／既契約／住所でポン／自社アポ禁）
- API `GET /api/soil/list/history?phone=`（manager 以上）：7 つの元を union して日付降順（同じ日は 種類の固定順）。コールは `system_call_history` の 1 件ずつ（2026-01〜）＋ `soil_list_call` の FM 取込行（出所='FM取込'）を「集約」として 1 行。最大 500 行（超えたら「古い分は省略」）。電話番号は数字のみに正規化して探す（携帯番号でも探せるよう `携帯番号` 列も見る）
- 番号が台帳に無いときも履歴（購入・投入・コール・自社アポ禁）は出す。何も無ければ「この番号の記録はありません」

## 5. テスト（vitest）
- migration 文面：表・列・索引・RLS
- API：登録（重複は足さない・理由必須・権限）・一括（プレビューの件数／番号の形／1,000 件ずつ）・解除・履歴検索の union と並び・携帯番号での検索
- 書き出し：`自社アポ禁` が除かれる・件数が結果と記録に出る（3 形式）
- 画面：管理方法タブの枠・絞り込みの既定「なし」・一覧の印・履歴検索タブの描画と種類の絞り
- 既存テストは全部通す

## 6. 変えてはいけないもの
- `AU光架電可否`・`アポ禁`・`クレーム履歴` の値と意味（決定 c）
- 分析の数字・集計・毎朝の自動処理
- Codex-329／330 の成果物

## 7. 受け入れ基準
1. vitest　2. tsc（今回由来のエラー無し）　3. eslint（変更ファイル）
4. Claude が：migration 適用 → 1 件登録／ファイル 3 件／解除 → 台帳の `自社アポ禁` → 絞り込み既定で消える → 書き出しで除かれ件数が出る → 履歴検索で 7 種類が時系列に出る（購入・投入・コールが揃う番号で）→ テスト行の削除・残 0

## 8. 完了報告（コードブロック）
- 「未コミット」／変更・追加ファイルのフルパス／migration のパス／受け入れ 1〜3／本番・git に触っていない明記／④ との差分（§10）

## 9. 画面文言
- 「自社アポ禁」「ヒュアラン社内だけの架電禁止。購入元のアポ禁とは別です」「登録」「解除」「解除済みも見る」「自社アポ禁 n 件を除きました」「履歴検索」「この番号の記録はありません」「古い分は省略しました」

## 10. 既知の差分（Codex が追記する）
