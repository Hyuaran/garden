# Codex-315 リストマスタ：「親／子」の呼び方をやめて 5 つの名前にそろえる＋管理方法タブを表に書き直す

作成日: 2026-09-11
作業ツリー: C:\garden\a-bloom-008
起点: main d9fe94d（`git log -1` で一致を確認してから着手）
**git は触らないこと（commit するな）。本番のデータに触らないこと。開発サーバ・ブラウザを起動しないこと。DB（migration）は今回ありません。**

必ず先に読むもの：
- ④正本：C:\Claude\000_Garden\150_System_システム\00_マニュアル\14_リストマスタ\Garden_System_リストマスタ_4_仕様書_Claude読み込み用.md（§3 データモデル・§8-1 投入の規則・§8-3 タブ）
- 画面：src/app/system/list/_components/ListMasterClient.tsx（`GUIDE_TEXT`・管理方法タブ・取り込み結果の文言）／list-master.module.css
- API の文言：src/app/api/soil/list/uploads/_lib/apply-upload.ts（`uploadStoppedMessage`）

## 0. 何を直すか（東海林さん 2026-09-11）

リストマスタの表を「親」「子」と呼ぶのをやめ、**次の 5 つの名前だけ**で呼ぶ。並び順も**この順**（管理方法タブ・説明文・今後の画面すべて）。

1. 電話番号台帳
2. 購入履歴
3. 投入履歴
4. コール履歴
5. 受注履歴（**新設予定。今回は「準備中」と表示するだけ**。表・取り込みは次の Codex-316 で作る）

あわせて、管理方法タブを**文章の箇条書きから表**に書き直す（読む人が表を 1 回見れば分かる形）。

## 1. 直すもの

### 1-1. 呼び方（画面に出る文言すべて）
- 「親」「子」「親（電話番号台帳）」「子：〇〇」を画面・API の返す文言から無くす。置き換えは：
  - 「親（電話番号台帳）：更新 n 件・新規追加 m 件…」→「**電話番号台帳：更新 n 件・新規追加 m 件…**」（ListMasterClient.tsx 328 行付近）
  - 「途中で止まりました（親へ反映 n / m）」→「**途中で止まりました（電話番号台帳へ反映 n / m）**」（ListMasterClient.tsx 336 行付近・apply-upload.ts 81 行付近の 2 か所とも）
  - `GUIDE_TEXT` の見出し・本文の「親」「子」
- 変えるのは**画面に出る文字だけ**。裏の表の名前（soil_list_phone／soil_list_purchase／soil_list_assignment／soil_list_call）、API の返す項目名（parent_updated など）、DB 関数名は**変えない**
- コード中のコメントの「親」は直さなくてよい（MultiSelectFilter.tsx の「枠（親）」は別の意味なので触らない）
- 既存テストが古い文言（「親へ反映」等）を見ていれば、新しい文言に合わせて直す

### 1-2. 管理方法タブを表にする
今の `GUIDE_TEXT`（見出し＋箇条書き）をやめ、**表 2 つ**にする。見出しは今の「リストマスタのデータの持ち方」のまま。

**表 1：5 つの表（この順・この中身）**

```
リストマスタのデータの持ち方
┌────────────┬──────────────┬──────────┬──────────────────────────┬──────────────────────────┐
│ 名前        │ 1 行の単位     │ 件数の目安  │ 入っているもの                │ いつ増える・変わるか            │
├────────────┼──────────────┼──────────┼──────────────────────────┼──────────────────────────┤
│ 電話番号台帳 │ 電話番号 1 件   │ 約 267 万件 │ 氏名・住所・郵便番号・携帯番号、  │ 同じ番号が別のリストで再び投入   │
│            │              │          │ いまのリスト名と投入日、         │ されたら、行を増やさずリスト名と │
│            │              │          │ AU光架電可否・アポ禁・購入状態、  │ 投入日を新しいものに書き換える。 │
│            │              │          │ コール回数・最終コール日         │ 氏名・住所などは空欄のときだけ埋める│
│ 購入履歴    │ 購入 1 回      │ 約 242 万件 │ 電話番号・購入先・購入日         │ 購入や開通のたびに 1 行増える。   │
│            │              │          │                          │ 過去の分もすべて残す            │
│ 投入履歴    │ 電話番号×リスト名│ 約 3.9 万件 │ 取込ファイルの列（申込者・連絡担当者│ アップロードのたびに増える。     │
│            │              │          │ ・既契約者・設置先など）そのまま   │ 同じ番号×リスト名は上書き        │
│ コール履歴  │ 電話番号×リスト名│ 約 119 万件 │ コール回数・初回／最終コール日・   │ 8/1 からはコールセンターの通話記録│
│            │              │          │ 最終結果                     │ を毎日集計して足す（丸い矢印で手動 │
│            │              │          │                          │ でも反映できる）                │
│ 受注履歴    │ 受注 1 件      │ 準備中     │ 準備中（Kintone「顧客一覧」の受注 │ 準備中                      │
│            │              │          │ を毎朝取り込む予定）             │                            │
└────────────┴──────────────┴──────────┴──────────────────────────┴──────────────────────────┘
```
- 件数の目安は**固定の文字**でよい（今の GUIDE_TEXT に書いてある数字を使う。投入履歴は「約 3.9 万件」）。画面を開くたびに数えない
- 「電話番号台帳へどう反映されるか」は、各行の「いつ増える・変わるか」の中に書く（列を増やしすぎない）。コール履歴の行には「電話番号台帳のコール回数・最終コール日はこの集計から作る」を入れる
- 受注履歴の行は、文字の色を薄くするなどして「まだ無い」ことが一目で分かるようにする

**表 2：決まり（項目｜決まり の 2 列）**

| 項目 | 決まり |
|---|---|
| リスト名 | 取込ファイルのものをそのまま使う（例：【光回線】フレッツ_20260907） |
| リスト投入日 | リスト名の中の日付（_20260907 の部分）。末尾の「_2」などは無視する |
| 投入日の意味 | 「その日から架電する日」。先の日付のリストは前もって入れておき、その日が来るまで架電しない。分析で架電済み率が低いリストは、まだ開始日が来ていないだけのことがある |
| 判定は投入で変えない | AU光架電可否・アポ禁・購入状態は、アップロードでは書き換えない |
| そのほかの表 | 保留（桁がおかしい番号など）・携帯のみ・絞り込みの選択肢・保存した条件・書き出しの記録・アップロードの記録・コール履歴の反映状態 |

- 今の GUIDE_TEXT の内容は**全部どこかの表に残す**こと（消える文があれば完了報告に書く）
- 表は横に長いので、狭い画面では表の枠の中で横にスクロールする（ページ全体を横スクロールさせない）。見た目は今のリストマスタの表（一覧の表）とそろえる
- 文言は業務の言葉のみ・絵文字なし（「migration」「RPC」などを画面に出さない）

## 2. テスト
- 管理方法タブ：表 1 の見出し 5 つがこの順で出る／「親」「子」の文字が出ない／受注履歴の行に「準備中」が出る／表 2 が出る
- 取り込み結果：完了時「電話番号台帳：更新…」、途中で止まった記録に「電話番号台帳へ反映 n / m」が出る（「親」が出ない）
- apply-upload のメッセージのテストがあれば新しい文言に合わせる
- 既存テスト（src/app/system/list・src/app/api/soil/list）は全部通す

## 3. 変えてはいけないもの
- 表・列・API 項目・DB 関数の名前（画面の文字だけ変える）
- アップロード・反映・絞り込み・書き出しの動き

## 4. 受け入れ基準
1. vitest（src/app/system/list・src/app/api/soil/list）が通る　2. tsc に今回の変更由来のエラーが無い　3. eslint（変更ファイル単位）が通る
4. Claude が本番で：管理方法タブが表 2 つで出る・画面のどこにも「親」「子」が残っていない、を確認

## 5. 完了報告（コードブロックで出力）
- 「未コミット」と明記／変更・追加ファイルのフルパス／受け入れ基準 1〜3 の結果／本番データ・git に触っていないことの明記／GUIDE_TEXT から表へ移さなかった文があればその文

## 6. 既知の差分（Codex が追記する）
