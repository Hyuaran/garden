# Handoff - 2026-05-07 20:35 (a-soil)

## 今やっていること

a-main-013 main- No. 86/88/91/93/95 連続 GO 受領、ガンガンモードで Batch 16 基盤実装 + Phase B-01 第 1 弾を完走。本セッションは a-main-013 main- No. 98 受領で締め、次セッションで Phase B-01 Extract / Load から再開予定。

## 次にやるべきこと（次セッション着手内容）

優先順:

1. **Phase B-01 Extract**（Kintone API client + cursor pagination skeleton、~0.5d）
   - 判断保留 1: Kintone API client パッケージ
     - a-leaf-002 / a-bud で `@kintone/rest-api-client` 利用しているか確認
     - 既存利用あれば同パッケージ採用、なければ fetch 自前実装推奨（新規 npm install 回避）
   - cursor pagination で 500 件 / req、`/k/v1/records/cursor.json` API
   - chunk size = 5,000 件（B-01 §5.1）
2. **Phase B-01 Load**（normalized → soil_lists upsert with chunked trx、~0.5d）
   - ON CONFLICT (source_system, source_record_id) で upsert
   - 1 chunk = 1 trx、失敗時はその chunk のみ rollback
3. **Phase B-01 Importer orchestrator class**（retry/resume 制御、~0.5d）
   - 判断保留 2: 実行環境 = Node.js script (npx tsx) + 進捗 DB resume 推奨（Edge は長時間ジョブ不向き）
   - paused → resume / failed → retry / running → cancel の状態遷移
4. **admin 進捗 UI**（/soil/admin/imports + SSE、~0.3d）
   - Server-Sent Events で chunk 完了を 5 秒間隔で更新
5. **leaf_kanden_cases.soil_list_id NOT NULL 化 migration**（B-01 バックフィル完走後）

## 注意点・詰まっている点

### 適用前確認事項（東海林さん認知済、apply 時に対応）

1. handle_pd_number_change の leaf_kanden_cases INSERT 列 = 最小 3 列（spec 6 列の保留は妥当判断）→ a-leaf 経由で実 schema 確認後に拡張
2. root_audit_log 書込 = 動的判定 + EXCEPTION 吸収（a-root 確定後に更新）
3. pg_trgm 拡張 = 他モジュールでも利用、drop しない
4. GIN trigram INDEX 作成時間 = 253 万件投入後は CONCURRENTLY 必須

### 判断保留 3 件（次セッション着手時に再確認）

| # | 論点 | 暫定判断 |
|---|---|---|
| 1 | Kintone API client パッケージ追加 | 既存 a-leaf-002 / a-bud で利用なら同パッケージ採用、なければ fetch 自前実装推奨（npm install 回避） |
| 2 | Importer 実行環境 | Node.js script (npx tsx) + 進捗 DB resume |
| 3 | PR merge タイミング | 通常 PR フロー（develop マージ） |

## 関連情報

### ブランチ

- **作業中ブランチ**: `feature/soil-batch16-impl`（origin push 済、本セッションで作成）
  - origin/develop ベース
  - 4 commits: Batch 16 #01-#08 + Phase B-01 staging + Transform
- **PR 中**: PR #127 `feature/soil-phase-b-decisions-applied-v2` → develop（OPEN、Batch 19 Phase B 7 spec）

### 関連ファイル

#### Migrations（7 本、すべて apply 未実施 = 東海林さん別途承認後）
- `supabase/migrations/20260507000001_soil_lists.sql`（spec #01 + #07 + B-03 関電列）
- `supabase/migrations/20260507000002_soil_call_history.sql`（spec #02 月次パーティション）
- `supabase/migrations/20260507000003_soil_rls.sql`（spec #06 RLS + MV + helper 関数）
- `supabase/migrations/20260507000004_leaf_kanden_soil_link.sql`（spec #03 leaf 列追加）
- `supabase/migrations/20260507000005_soil_indexes.sql`（spec #05 性能追加 INDEX + pg_trgm）
- `supabase/migrations/20260507000006_soil_handle_pd_number_change.sql`（B-03 §6 DB 関数）
- `supabase/migrations/20260507000007_soil_imports_staging.sql`（B-01 §3.3 staging tables）

#### TypeScript（3 ファイル）
- `src/lib/db/soil-types.ts`（DB 行型定義 + isValidSupplyPoint22）
- `src/lib/db/soil-helpers.ts`（normalizePhone / normalizeKana / buildSoftDeletePayload）
- `src/lib/db/soil-import-transform.ts`（Kintone App 55 → SoilListInsert 変換）

#### Tests（3 ファイル、46 ケース全 PASS）
- `src/lib/db/__tests__/soil-types.test.ts`（8 ケース、isValidSupplyPoint22）
- `src/lib/db/__tests__/soil-helpers.test.ts`（23 ケース、normalize/softDelete）
- `src/lib/db/__tests__/soil-import-transform.test.ts`（15 ケース、Kintone transform）

### 関連 spec

- **Batch 16 基盤**（origin/develop merged）: `docs/specs/2026-04-25-soil-{01-08}-*.md`（8 件）
- **Batch 19 Phase B**（PR #127 OPEN）: `docs/specs/2026-04-26-soil-phase-b-{01-07}-*.md`（7 件）

### 累計成果（本セッション 4 commit）

- migrations: 7 本（lists / call_history / RLS / kanden_link / indexes / pd_number_change / staging）
- TypeScript: 3 ファイル
- Vitest: 46 ケース全 PASS
- 行数: 2,800+ insertions
- 工数換算: spec 工数合計 ~6.5d → 実績 ~5h（13 倍速）

### Batch 16 進捗

- ✅ #01 リスト本体スキーマ → 20260507000001
- ✅ #02 コール履歴 → 20260507000002
- ✅ #03 関電 Leaf 連携 → 20260507000004 + 000006
- ⏳ #04 インポート戦略 → Phase B-01 で実装中（staging + Transform 完了、Extract/Load 残）
- ✅ #05 インデックス性能 → 20260507000005
- ✅ #06 RLS → 20260507000003
- ✅ #07 削除パターン → soil_lists 列 + soil-helpers
- ✅ #08 API 契約 → soil-types.ts

### Phase B-01 進捗

| 段階 | 状態 |
|---|---|
| staging schema | ✅ 完了 |
| Transform 関数 + TDD | ✅ 完了 |
| Extract（Kintone API client） | ⏳ 次セッション |
| Load（normalized → soil_lists upsert） | ⏳ 次セッション |
| Importer orchestrator class | ⏳ 次セッション |
| admin 進捗 UI | ⏳ 次セッション |

### 関連 dispatch（本セッション分）

- main- No. 83（横断指示 認証統一）→ soil-3 受領確認（Soil 対象外）
- main- No. 86（横断指示 ガンガン進める）→ soil-4 判断保留（PR #101 消失）
- main- No. 88（Q1-Q3 回答）→ PR #127 発行 + Batch 16 第 1 弾着手 → soil-38 報告
- main- No. 91（残作業継続 GO）→ Batch 16 第 2 弾 → soil-39 報告
- main- No. 93（Batch 16 第 2 弾完走 評価 + 残作業 GO）→ Batch 16 第 3 弾 → soil-40 報告
- main- No. 95（Batch 16 完成 評価 + Phase B-01 着手 GO）→ Phase B-01 第 1 弾 → soil-41 報告
- main- No. 98（新セッション推奨採用）→ 本 handoff doc 作成 → soil-42 受領確認（次が本書）

## 担当セッション

a-soil（次セッションも同名で継続）

## dispatch counter: 次番号 42

次セッション起動時、`docs/dispatch-counter.txt` に 42 が書き込まれていることを確認。
本 handoff の受領確認発信は soil-42 で送信済（counter は 43 に更新する想定）。
