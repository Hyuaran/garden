# Handoff - 2026-05-07 (a-bloom-003 → a-bloom-004)

## 引継ぎ理由
Desktop アプリで a-bloom-003 セッションが「PR マージ後 自動アーカイブ」設定により誤アーカイブ → サイドバーから消失、UI 復元手段なし。a-bloom-004 worktree に引っ越して continuity 確保。

Auto-archive 設定は **OFF 済み**（2026-05-07 17:08 時点、Customize 内 Settings 経由）。004 で再発無し。

---

## 今やっていること
5/8 後道さんデモ向け Bloom 6 画面 + /bloom/progress 実データ化 + Vercel 本番反映の最終仕上げ。a-main-013 dispatch 待機状態。

## 次にやるべきこと
1. **a-main-013 main- No.80+ 待ち**（main- No.77/78/79 完了済、次の指示待機）
2. main- No.78 で残った課題: Vercel 本番の `X-Data-Source: mock`（Vercel env に `SUPABASE_SERVICE_ROLE_KEY` 未設定の可能性）→ a-main-013 / 東海林さん側の Vercel ダッシュボード操作待ち
3. main- No.79 で BloomState dev mock を feature/bloom-6screens-vercel-2026-05 に push 済、Vercel 本番への反映は別判断（dispatch あれば実施）

## 注意点・落とし穴

### 🔴 削除禁止ルール（絶対遵守）
- 削除コマンド (`rm`, `Remove-Item`, `del`) は **system deny**
- ファイル更新時は `*.legacy-<context>-YYYYMMDD.{tsx,html,ts,sql}` で必ず legacy 保持
- 既に 24 件の legacy ファイル蓄積（main- No.41/42/44/45/48/50/56/58/60/61/77/79）

### 🔴 dev server 起動 → webpack 必須
`package.json`: `"dev": "next dev --webpack"` （Turbopack ではなく webpack）。`npm run dev > dev.log 2>&1 &` 形式で起動推奨。pipe に注意（`| head` 等で truncate されると即終了）。

### 🔴 BloomGate dev バイパス維持
`process.env.NODE_ENV === "development"` で BloomGate 通過。BloomState も同条件で dev mock 動作（main- No.79）。

### 🔴 dispatch v4 ヘッダー形式 厳守
- `<アイコン> bloom-003- No. NNN`
- 発信日時 = `Get-Date -Format "yyyy-MM-dd(ddd) HH:mm"` 実時刻必須
- メイン投下用は `~~~` でラップ
- 各送信ごとに `docs/dispatch-counter.txt` を +1

### 🟡 worktree 別作業
a-bloom-003 は **アーカイブされたまま JSONL 永続保存**。サイドバーには出ないが、過去ログは `~/.claude/projects/C--garden-a-bloom-003/` に保存済み。誤って同時起動するとファイル競合の可能性、原則 a-bloom-004 に集中。

---

## 関連情報

### ブランチ
**`feature/bloom-6screens-vercel-2026-05`**（a-bloom-003 と共有 worktree）

直近 commit:
- `873a257` docs(bloom): bloom-003- No.39 completion report (main- No.79 dev mock)
- `16f7e42` fix(bloom): BloomState dev mock 整備 (Workboard 等の loading 滞留解消) [dispatch main- No.79]
- `87fdca2` docs(bloom): bloom-003- No.37 completion report (main- No.77 bugfix)
- `41580d3` fix(bloom): daily-report の display_name → name に修正 (Vercel build error 解消)
- `16fafa5` fix(bloom): /api/bloom/progress-html 500 修正 + Supabase 実データ表示 [dispatch main- No.77]

### main / develop 反映状況
- main HEAD: `30aa992`（Merge PR #126 hotfix）
- main- No.79 の dev mock は **feature ブランチのみ**（main へは未反映、a-main-013 判断待ち）

### dispatch 履歴サマリ（直近）
| dispatch | 内容 | 完了 commit |
|---|---|---|
| main- No.77 | /api/bloom/progress-html 500 修正 + Supabase マッピング | `16fafa5` |
| main- No.78 | feature → develop → main PR + Vercel 自動デプロイ | `30aa992`（hotfix 込み）|
| main- No.79 | BloomState dev mock 整備 | `16f7e42` |

### 重要ファイル
| File | 役割 |
|---|---|
| `src/app/api/bloom/progress-html/route.ts` | v29 動的描画 API（DB マッピング + mock fallback + 二段 fallback）|
| `src/app/bloom/_state/BloomStateContext.tsx` | dev mock 注入（DEV_MOCK_USER）|
| `src/app/bloom/{progress,daily-report,ceo-status,workboard,monthly-digest}/layout.tsx` | metadata.title 個別化 |
| `src/app/bloom/_components/BloomShell.tsx` | NAV_ITEMS 5 件（progress 含む）|
| `src/app/bloom/_components/BloomSidebar.tsx` | NAV_PAGES 5 件 |
| `public/_proto/bloom-dev-progress/index.html` | v29 テンプレート（base href + placeholder マーカー）|

### Supabase 状態（a-main-013 環境）
- root_daily_reports: 3 行（4/22-24 + 4/25 取込済み）
- root_daily_report_logs: 31 行
- root_module_progress: 12 行
- localhost で `X-Data-Source: supabase` 動作確認済
- Vercel は `X-Data-Source: mock`（env 未設定推定、要確認）

### dev server 状態
- ローカル PID 16084（webpack mode）または再起動必要
- 起動コマンド: `: > dev.log && npm run dev > dev.log 2>&1 &`

### dispatch counter
**次番号 41**（bloom-003- No. 40 まで送信済 = 本 handoff 兼引越通知）

### legacy ファイル一覧（削除禁止対象、24 件累計）
過去の handoff `docs/handoff-20260505-bloom-002.md` + 各 dispatch の完了報告に記載済。一覧は `git ls-files | grep legacy` で抽出可能。

---

## 起動チェックリスト（a-bloom-004 用）

```bash
cd C:\garden\a-bloom-004
pwd
git status
git branch --show-current   # → feature/bloom-6screens-vercel-2026-05
git log --oneline -3        # → 873a257 docs(bloom): bloom-003- No.39 completion report
ls .env.local node_modules  # 両方存在（junction + copy 済みなら OK）

# dev server 起動
: > dev.log && npm run dev > dev.log 2>&1 &
sleep 5
curl -s -o /dev/null -w "%{http_code} | /bloom\n" --max-time 60 http://localhost:3000/bloom
curl -s -o /dev/null -w "%{http_code} | /api/bloom/progress-html\n" --max-time 30 http://localhost:3000/api/bloom/progress-html
```

期待: 全 200 + X-Data-Source: supabase（ローカルは Supabase 接続済）

## 次の発信
a-bloom-004 起動後、a-main-013 へ「bloom-004- No. 1（旧 bloom-003- No. 41）引継ぎ完了通知」発信推奨。dispatch counter は 41 から継続。
