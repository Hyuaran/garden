# Handoff — 2026-04-26 15:23 / a-bud (A) Bud Phase D follow-up 3 件

## セッション概要

- **担当**: a-bud (A) / ブランチ `feature/bud-phase-d-specs-batch17-auto-fixes`
- **モード**: GitHub suspended 継続、ローカル commit のみ（push 不可）
- **対応**: a-main 006 確定後の東海林さん再判断 3 件を反映
- **直前の状態**: commit `3e5a6a4`（Kintone 解析 #16-#19 + #25 反映）

## 完了した作業

### #1 D-01 給与期間 大幅修正

**ファイル**: `docs/specs/2026-04-25-bud-phase-d-01-attendance-schema.md`

**変更**:
- §2.2 期間管理: 「当月 21 日〜翌月 20 日（KoT 既存運用）」→「**1 日〜月末** / 翌月末営業日 / 明細 20-25 日」に全面改訂
- §3.1 `bud_payroll_periods`: `statement_publish_target_date` 列追加、コメント更新
- §9 判保留: 判 1 を 1 日〜月末 + root_settings 化に書換、判 7-8 追加（履歴管理 / 自動配信化）
- 新セクション: `root_settings` 候補列定義（4 キー）+ admin 画面から再起動なしで変更可能

**ポイント**:
- 給与計算期間定義を `root_settings` 化、Phase E での自動化（明細自動配信 Cron 等）対応設計
- 法人別 `root_settings` で個別調整可（既存判 2 と整合）
- 設定変更履歴は `root_settings_history`（Root Phase B 連携、別 spec で起票）

### #2 D-06 年末調整精算月 = 1 月精算

**ファイル**: `docs/specs/2026-04-25-bud-phase-d-06-nenmatsu-integration.md`

**変更**:
- §2 全体フロー: 12 月精算 → 翌年 1 月精算へ書換、採用理由 3 点明記（年末手取り安定 / 賞与分離 / 退職者対応シンプル）
- §2.1 退職者の例外フロー新設（旧設計 vs 新設計の対比表）
- §5 タイトル: 「12 月給与での精算」→「翌年 1 月給与での精算」改題、フロー改訂
- §10 判保留: 判 1 を 1 月精算へ書換、判 7 追加（月次源泉計算順序）
- §11.4 1 月給与の還付額警告閾値の引き下げ検討（30% → 20%）
- §11.5 退職者ルール簡素化（12 月末退職者は最終給与で即時精算、1 月精算対象外）

**ポイント**:
- 12 月手取りは月次源泉のままで安定、1 月給与で精算反映
- 12 月末退職者は「月途中退職と同じ即時精算」フローに統一可（例外削減）

### #3 D-07 振込ファイル = ハイブリッド方式

**ファイル**: `docs/specs/2026-04-25-bud-phase-d-07-bank-transfer.md`

**変更**:
- §2 振込パターン: 「30 件閾値で個別/FB 切替」→「**ハイブリッド方式**（件数によらず FB 1 ファイル + 勘定項目別レポート CSV）」に全面改訂
- §3.3 `bud_payroll_accounting_reports` テーブル新設
  - `category_breakdown` jsonb（5 区分: 給与 / 外注費 / 通勤手当 / 賞与 / 役員報酬）
  - `imported_to_mf_at` で MFC 取込確認時刻を記録
- §3.4 CSV フォーマット定義（UTF-8 BOM 付き、マネーフォワードクラウド会計取込形式）
- §4.1 連携フロー全体図をハイブリッド出力に書換（旧 30 件閾値判定廃止）
- §10 判保留: 判 1 をハイブリッド方式に書換、レポート粒度の Phase E 調整可性を明記

**ポイント**:
- 銀行手数料: 件数によらず 1 取引分に節約
- 後道さん運用: マネーフォワードクラウド会計に直接取込で勘定項目別仕訳
- Phase E で粒度調整可（5 区分 → 細分化 / 統合）

### effort-tracking 追記
- 3 行追加（#1 / #2 / #3 の実績、各 0.05〜0.15d）

## ブランチ状態（local commits の累積）

| commit | 内容 |
|---|---|
| `d1483ac` | PR #74 a-review #1-#5 改修 |
| `94738a8` | D-04 Y 案 + フォールバック確定 |
| `3e5a6a4` | Kintone #16-#19 + #25 反映 |
| 本 commit | **follow-up 3 件**（D-01 / D-06 / D-07） |

## 注意事項遵守

- ✅ main / develop 直 push なし（GitHub suspended で push 自体不可）
- ✅ Supabase 本番への書込なし
- ✅ ファイル削除なし
- ✅ 動作変更なし（spec 改訂のみ、コード変更なし）
- ✅ a-auto / a-soil / a-root のブランチ非接触
- ✅ D-02 / D-04 / D-05（本体）非接触、連動修正のみ
- ✅ D-09 / D-10 / D-11（直前起票分）非接触

## 連動確認結果

- D-02 給与計算: D-01 期間定義変更を参照する箇所はテーブル列名のみ（`payroll_period_id` リンク） → リンク列は不変、修正不要
- D-03 賞与計算: 同上、修正不要
- D-08 テスト戦略: 期間定義のテストケースは「期間定義に応じた検証」と抽象記述、修正不要

## GitHub 復旧後のアクション

1. ブランチ全 commit を push
2. PR #74 のレビューコメント突き合わせ
3. **後道さん協議**: 勘定項目別レポートの粒度確定（D-07 §3.3、5 区分 → 細分化 / 統合）
4. **東海林さん協議**:
   - `root_settings` 設定変更画面の UI 設計（D-01 判 7）
   - 明細自動配信化のタイミング（D-01 判 8、Phase D or E）
   - 1 月給与の還付額警告閾値（D-06 §11.4、20% or 30%）

## 関連ドキュメント

- 確定ログ: `C:\garden\_shared\decisions\decisions-kintone-batch-20260426-a-main-006.md`
- follow-up: `C:\garden\_shared\decisions\spec-revision-followups-20260426.md` §2 #1-#3
- 関連 memory:
  - `project_mfc_payroll_csv_format.md`
  - `feedback_kintone_app_reference_format.md`
  - `project_payslip_distribution_design.md`

## 次セッションでの最初のアクション

1. GitHub サポート復旧確認
2. 復旧していたら累積 4 commits を push
3. PR #74 状態確認 + レビュー突き合わせ
4. 後道さん / 東海林さん協議事項整理
