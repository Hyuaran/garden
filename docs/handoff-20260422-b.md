# ハンドオフ — 2026-04-22〜23 B (b-main) → A 引継ぎ (Garden-Tree)

**担当セッション名**: b-main (B)
**作業期間**: 2026-04-22 〜 2026-04-23 朝
**引継ぎ理由**: B セッション区切り、A へ引き継ぎ
**モジュール**: Garden-Tree（架電アプリ）

---

## 今やったこと

Garden-Tree の **Phase B / Phase B-β** を完遂。MVP として架電結果の永続化＋新規加入者誘導フローが揃った状態。

1. **Phase B**: 架電 4 画面（Sprout / Branch / 通常通話 / フォローコール）の通話結果を `soil_call_history` に保存する機能を実装。call_mode を画面別に切替（`sprout` / `branch` / `confirm_pre` / `confirm_post` / `follow`）。留守/不通ボタンは `startAt=endAt=now` で 0 秒保存。通話成立時は `connectedAt` / `hangupAt` 記録、見込 A/B/C・コインの `next_contact_at`、NG その他の `ng_reason`、架電メモを保存。([PR #5](https://github.com/Hyuaran/garden/pull/5) merged)
2. **Phase B-β**: 初回ログイン時に `root_employees.birthday` が null なら `/tree/birthday` に誘導して誕生日入力させる画面を追加。TreeAuthGate に誕生日ガードを追加、TreeShell で `/tree/birthday` を bare screen 扱い（サイドバー・KPI ヘッダー非表示）。([PR #6](https://github.com/Hyuaran/garden/pull/6) merged)
3. **動作確認済**: `1324 三好 理央` で birthday を null に設定 → `/tree/login` → `/tree/birthday` リダイレクト → 誕生日保存 → `/tree/dashboard` 遷移 / Supabase `root_employees.birthday` が YYYY-MM-DD で UPDATE されることを目視確認

---

## 次にやるべきこと

### Phase C（次の MVP 拡張） — 要ブレスト
Phase B-β で「架電→結果保存→誕生日誘導」の最小ループが完成。次に何をやるかは未確定で、候補は以下：

1. **Tree 他画面の Supabase 連携**
   - プロトタイプ段階の画面（`alerts` / `feedback` / `monitoring` / `videos` / `chatwork` / `breeze` / `search` / `scripts` / `break-schedule` / `confirm-wait` / `toss-wait` / `wrap-up` 等）をそれぞれ実データ接続
2. **KPI ヘッダー・ダッシュボードの実データ化**
   - 現状 `_state` の mock 値。`soil_call_history` から集計する方式に切替
3. **Bloom 連携**
   - 案件一覧 VIEW や日報 KPI で Tree のコール履歴を横断参照
4. **Phase B/B-β のフィードバック対応**
   - 本番運用で出た修正・調整

### 必須の事務作業

- **`docs/effort-tracking.md` に Tree 実績を遡及追記**
  - 現状 Forest のみ記録。Phase A / 1 / B / B-β の見積/実績を追加する（メモリ `feedback_effort_tracking.md` 準拠）
- **Tree の spec / plan ドキュメントを作成**
  - Forest は `docs/superpowers/specs/` `plans/` に設計書あり。Tree は**0 件**。Phase C 着手前に最低限 spec は書きたい

---

## 注意点・詰まっている点

### 🔴 このブランチと実作業ブランチが違う（重要）
- **このハンドオフがある場所**: `feature/tree-phase-b`（PR #5 merged の元）
- **B 作業の実コード最新**: `feature/tree-phase-b-beta` の `a2e925c feat(tree): Phase B-β 誕生日入力画面を追加`
- **A が続きをやるなら `feature/tree-phase-b-beta` を base にすること**。`feature/tree-phase-b` 単体だと B-β が抜ける

### 🟡 Tree の spec / plan / handoff ドキュメントが 0 件
- 他モジュール（Forest / Bud / Root）は `docs/superpowers/specs/` `plans/` に設計書あり
- Tree は設計書無しでいきなり実装が進んだ状態。Phase C 着手前に簡易 spec を書く推奨

### 🟡 CLAUDE.md ドリフト
- メモリ `project_garden_claudemd_drift.md` 参照
- b-main 側の CLAUDE.md は古く運用 3 章が欠落。**a-main 版を正**とする

### 🟡 `call_mode` の列挙値が src 内ハードコード
- `sprout` / `branch` / `confirm_pre` / `confirm_post` / `follow` の 5 種類が TypeScript 側にのみ存在
- Supabase 側に enum or check constraint を追加するか検討（現状は文字列そのまま保存）

### 🟢 Phase A〜B-β で検証済み
- 認証・権限切替（admin / manager / staff / super_admin）
- ログイン→ダッシュボード遷移
- 架電 4 画面の保存フロー（4/4 成功）
- 誕生日 null → 誘導 → 保存→ /tree/dashboard の全経路

---

## 関連ファイル・ブランチ名

### ブランチ
- **このハンドオフの場所**: `feature/tree-phase-b`（A→B の元ブランチ）
- **B 作業の実コード最新**: `feature/tree-phase-b-beta`（PR #6 merged）
- **次セッション推奨**: `feature/tree-phase-b-beta` から新ブランチ派生 or `develop` から最新を切る
  - 例: `feature/tree-phase-c`、`feature/tree-dashboard-live-kpi`

### 主要ファイル（Phase B / B-β で変更）

**Phase B（通話履歴保存）**
- `src/app/tree/calling/sprout/page.tsx` — Sprout 保存（call_mode=sprout）
- `src/app/tree/calling/branch/page.tsx` — Branch 保存（call_mode=branch, RESULT_BUTTONS グリッド）
- `src/app/tree/call/page.tsx` — 通常通話・前確後確・フォロー保存（mode に応じて call_mode 切替）
- `src/app/tree/follow-call/page.tsx` — /tree/call?mode=follow への遷移

**Phase B-β（誕生日入力）**
- `src/app/tree/birthday/page.tsx` — 新規作成（date input、updateBirthday、refreshAuth）
- `src/app/tree/_components/TreeAuthGate.tsx` — 誕生日ガード追加
- `src/app/tree/_components/TreeShell.tsx` — /tree/birthday を bare screen 扱い
- `src/app/tree/_constants/screens.ts` — TREE_PATHS.BIRTHDAY 追加

**共通（Phase A〜B 累積）**
- `src/app/tree/_lib/` — 認証・mutation ユーティリティ、auth gate、permissions
- `src/app/tree/_components/` — Sidebar / KPI Header / QuadTimer / ConfirmSidePanel
- `src/app/tree/_constants/` — colors / roles / sounds / screens

### Supabase
- **参照テーブル**: `soil_call_history` (write), `root_employees` (birthday UPDATE)
- **RLS**: 本人のみ自分の birthday UPDATE 可能（既存ポリシーで担保）
- **call_mode 値**: `sprout` / `branch` / `confirm_pre` / `confirm_post` / `follow`（TypeScript ハードコード）

---

## A セッション再開時の推奨コマンド

```powershell
# 1. 最新を取得
cd C:\garden\a-main
git fetch --all

# 2. Tree 継続作業は beta ブランチから派生
git checkout feature/tree-phase-b-beta
git pull origin feature/tree-phase-b-beta

# 3. 新ブランチ切る場合
git checkout -b feature/tree-phase-c

# 4. Claude Code 起動
claude
```

起動後、以下を依頼:

> `feature/tree-phase-b` の `docs/handoff-20260422-b.md` を読んで、Tree の次フェーズ（Phase C）をブレストから進めて

---

## Phase ロードマップ（Tree 全体）

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase A | 認証・権限 Supabase 連携 | ✅ 完了（PR #4） |
| Phase 1 | 表記揺れ修正＋マイページ定期確認ロック | ✅ 完了（PR #4 内） |
| Phase B | 架電 4 画面の通話結果を `soil_call_history` に保存 | ✅ 完了（PR #5） |
| Phase B-β | 初回ログイン時の誕生日入力画面 | ✅ 完了（PR #6） |
| Phase C-? | Tree 他画面 Supabase 連携 / KPI ライブ化 / Bloom 連携 | ⏳ 未着手・要ブレスト |
