# ハンドオフ — 2026-04-22〜23 B (b-main) → A 引継ぎ (Garden-Root)

**担当セッション名**: b-main (B)
**作業期間**: 2026-04-22 夕方 〜 2026-04-23 朝
**引継ぎ理由**: B セッション区切り、A へ引き継ぎ
**モジュール**: Garden-Root（組織・従業員・パートナー・マスタデータ）

---

## 今やったこと

Garden-Root **Phase 1（認証・権限管理）** の実装を完了。コードレビューも APPROVED。実作業は別ブランチ `feature/root-auth-ui` に全コミットが入っている（PR 作成待ち）。

- 7 マスタ（companies / bank_accounts / vendors / salary_systems / insurance / attendance / root_employees）の RLS を `root_can_access` / `root_can_write` に統一
- 認証 UI: `/root/login` ログイン画面、セッションタイマー、自動ログアウト、監査ログ記録
- ロール権限: super_admin / admin 編集可、manager 閲覧のみ、staff 拒否
- 自動検証 7/13 シナリオ合格（残 6 は東海林さんの目視確認待ち）
- レビュー指摘対応: login page returnTo 二重消費修正、`root_audit_log` RLS ポリシー追加

---

## 次にやるべきこと

### 即時（東海林さん側）
1. **PR 作成**: https://github.com/Hyuaran/garden/pull/new/feature/root-auth-ui
   - base: `develop`
   - title 例: `feat(root): Phase 1 認証・権限管理`
2. **最終 UX 確認（残 6 シナリオ、計 10-15 分）**:
   - シナリオ 1: `http://localhost:3000/root/login` で `0008 東海林` ログイン → 編集可能
   - シナリオ 2: `0000 後道`（admin） ログイン → 編集可能
   - シナリオ 4: 法人マスタで代表者名編集 → 保存 → `root_audit_log` に `master_update` 記録を Supabase Dashboard で確認
   - シナリオ 12: ログイン後タブを**前面のまま 20 秒放置** → 警告モーダル出現 → [作業を続ける] でリセット
   - シナリオ 13: DevTools → Network → Offline でログイン試行 → 通信エラー表示
3. develop へマージ → Vercel dev 自動デプロイ確認

### 次セッション（Phase 2 着手）
**Phase 2: 他アプリからの参照ルール整備**
- Bud / Leaf モジュールから Root マスタを参照するクエリを追加
- RLS が期待通りに機能するか確認
- Tree Phase A で `root_employees` RLS は整備済み

**Phase 3 以降**: KING OF TIME 連携 → 法人連携表 → MF クラウド給与連携

---

## 注意点・詰まっている点

### ⚠️ dev モードタイマー短縮（忘れやすい）
- `src/app/root/_lib/session-timer.ts` で `NODE_ENV === "development"` のとき **30 秒**タイムアウト
- Vercel 本番は自動で `production` になり **2 時間**に切替
- ローカル `npm run dev` で試す時は 30 秒で切れる前提で操作

### 🟡 Bud 設計書紛失の可能性
- `2026-04-17-bud-design-v2.md` が `feature/bud-phase-0-auth` ブランチに **push されていない**（引越し時の紛失疑い）
- Bud Phase 1 着手時に確認・再作成が必要

### セッション中に修正した既存バグ
- **`root_audit_log` は RLS 有効だがポリシー未設定**だった → `scripts/root-rls-phase1-audit-fix.sql` を適用済み
- **login page の returnTo が二重消費される不具合** → Phase 1 中に修正済み

### 自動検証済シナリオ（7/13）
| # | 内容 | 結果 |
|---|---|---|
| 3 | manager ログイン → 閲覧のみ・write ボタン disabled + tooltip | ✅ |
| 5 | ログアウト後再ログインで元画面復帰 | ✅ |
| 6 | 手動ログアウト | ✅ |
| 7 | staff ログイン拒否 | ✅ |
| 8 | 存在しない社員番号でログイン試行 | ✅ |
| 9 | 正しい社員番号 + 誤パスワード | ✅ |
| 11 | 30秒無操作で自動ログアウト | ✅ |

監査ログ動作確認済: `login_failed` / `logout` / `login_success` / `login_denied` の 4 アクションが `root_audit_log` に記録されることを目視確認。

---

## 関連ファイル・ブランチ名

### ブランチ
- **A→B 時点の元ブランチ（このファイルがある場所）**: `feature/root-handoff-a-20260422`
- **B 実作業ブランチ**: `feature/root-auth-ui`（develop から分岐、19 コミット先行、PR 作成待ち）
  - 内訳: 設計書 1 / プラン 1 / 実装 14 / レビュー修正 3
- **次セッション推奨**: `feature/root-auth-ui` を develop へマージ後、Phase 2 用の新ブランチを develop から派生

### 関連ドキュメント（`feature/root-auth-ui` 上）
- 設計書: `docs/superpowers/specs/2026-04-22-root-auth-phase-1-design.md`
- プラン: `docs/superpowers/plans/2026-04-22-root-auth-phase-1-plan.md`
- 詳細ハンドオフ: `docs/handoff-20260423.md`（同ブランチ最新コミット `05bf2c5`）

### Supabase 適用済 SQL
- `scripts/root-rls-phase1.sql`（7 マスタ RLS 更新）
- `scripts/root-rls-phase1-audit-fix.sql`（`root_audit_log` RLS 追加）

### 現在の登録アカウント（11 名、Tree Phase A 作成分）
| 社員番号 | 氏名 | ロール | Root ログイン |
|---|---|---|---|
| 0000 | 後道 | admin | ✅ 編集可 |
| 0004 | 上田 基人 | admin | ✅ 編集可 |
| 0008 | 東海林 美琴 | super_admin | ✅ 編集可 |
| 0009 | 萩尾 拓也 | staff | ❌ 拒否 |
| … | … | … | … |

---

## A セッション再開時の推奨コマンド

```powershell
# 1. 最新の develop を取得
cd C:\garden\a-main
git fetch --all
git checkout develop
git pull origin develop

# 2. feature/root-auth-ui の PR 作成・マージ対応
git checkout feature/root-auth-ui
git log --oneline -5  # 最新が 05bf2c5 docs(root): Phase 1 完了ハンドオフメモ であることを確認

# 3. Claude Code 起動
claude
```

起動後、以下を依頼:

> `docs/handoff-20260422-b.md` を読んで、feature/root-auth-ui の PR 作成と Phase 1 最終 UX 確認（残 6 シナリオ）から進めて

---

## Phase ロードマップ（Root 全体）

| フェーズ | 内容 | 状態 |
|---|---|---|
| Phase 0 | 基盤設計（7 マスタ・RLS 骨組み） | ✅ 完了 |
| Phase 1 | 認証・権限管理（ログイン UI・セッションタイマー・監査ログ） | ✅ 完了（PR 作成待ち） |
| Phase 2 | 他アプリからの参照ルール整備（Bud / Leaf 連携） | ⏳ 未着手 |
| Phase 3 | KING OF TIME 連携 | ⏳ 未着手 |
| Phase 4 | 法人連携表（Forest との `company_link`） | ⏳ 未着手 |
| Phase 5 | MF クラウド給与連携 | ⏳ 未着手 |
