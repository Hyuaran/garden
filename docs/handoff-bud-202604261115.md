# Handoff — 2026-04-26 11:15 / a-bud (A) a-review 重大指摘修正セッション

## セッション概要

- **担当**: a-bud (A) / 2 ブランチ並行作業
- **モード**: GitHub サポート復旧待ち、ローカル commit のみ（push なし）
- **対応 PR**: #55（Bud Phase A-1 完結）+ #74（Bud Phase D 給与処理 spec）
- **a-review コメント**: 直接読めず、自己分析で重大指摘を特定

## 完了した作業

### PR #55 修正（commit `81eb0cb`、ブランチ `feature/bud-phase-0-auth`）

a-review 重大指摘 3 件 + 関連 1 件（B4）を自己分析で特定し修正。

**B1 (金銭関連 RLS)**: 起票者本人の「下書き → 確認済み」遷移を禁止
- 旧 policy 2（`bud_transfers_update_draft_self_or_admin`）が status='下書き' の全 UPDATE を起票者 or admin に許可 → 起票者本人が「確認済み」へ遷移可能で二重チェック原則違反
- `scripts/bud-rls-v3-review-fixes.sql` で policy 2 を分割:
  - 2a `bud_transfers_update_draft_edit`: 下書き内編集
  - 2b `bud_transfers_update_draft_to_confirmed`: 下書き → 確認済み（admin+ かつ起票者以外）

**B2 (状態遷移違反)**: 確認済み → 差戻し を RLS から不許可化
- canTransition では `確認済み: ['承認待ち', '下書き']` のみ許容
- 旧 policy 3 の WITH CHECK が '差戻し' を含んでいた
- v3 で WITH CHECK から '差戻し' 除外、'下書き' に置換

**B3 (二重実装・監査漏れ)**: TS 層レガシー関数を `@deprecated` 注釈
- `selfApproveAsSuperAdmin` / `updateTransferStatus` は直接 UPDATE で `bud_transfer_status_history` への INSERT がスキップ → 監査漏れ
- 現時点で src/ 内呼出 0 件、新規呼出は `transitionTransferStatus()` を使用

**B4 (関連修正、A-05 spec §6 整合)**: 振込完了マークを admin+ に緩和
- A-05 spec §6 役割表で admin+ が CSV出力済み → 振込完了 を実行可
- 旧 policy 7 が super_admin only で業務上 admin がブロック
- v3 で `bud_transfers_update_complete_admin` に置換

**東海林さん手動再適用**:
- `scripts/bud-rls-v3-review-fixes.sql` を Supabase SQL Editor で実行（policy DROP + 再作成）

**テスト・型**: 919 tests 緑、`tsc --noEmit` 0 エラー（動作非破壊）

### PR #74 修正（commit `d1483ac`、新ブランチ `feature/bud-phase-d-specs-batch17-auto-fixes`）

a-review 配信経路セキュリティ重大指摘 5 件を自己分析で特定し spec 改修。

**#1 PDF パスワード脆弱**: MMDD 4 桁 = 366 候補 / 社員番号下 4 桁 = 10000 候補でブルートフォース容易
- A-07 採択時の方式 2「生年月日 4 桁 or 社員番号下 4 桁」は PDF AES-256 暗号化を無効化
- §6.5.1 / §6.5.2 に推奨 3 案を提示:
  - **A 案 (推奨)**: 強ランダム 16 文字 PW + 別経路通知（Chatwork DM / SMS）
  - **B 案**: 一回限りダウンロードトークン方式
  - **C 案 (非推奨)**: 現状維持
- **🟡 東海林さん最終判断要**（A-07 採択結果の見直しに直結）

**#2 SPF/DKIM/DMARC 未定義**: §6.4.1 で必須設定明記
- SPF hard fail / DKIM 2048bit / DMARC `p=reject` (専用サブドメイン)
- 実装着手前に DNS 反映を `dig` 等で疎通確認

**#3 SMTP TLS / Storage パス**: §6.4.2 + §6.6.2
- SMTP TLS 1.2 以上必須（プロバイダ管理画面で確認）
- Storage パスを `{employee_id}/{statement_id_uuid}.pdf` に変更（列挙対策）

**#4 メール本文 PW 平文同送**: §6.3 改訂
- 「PW は生年月日 4 桁です（例: 1985年3月15日 → 0315）」を削除
- 「別送した SMS / Chatwork DM をご確認ください」に変更

**#5 ダウンロード Server Action**: §6.6.1 + §6.6.2
- Rate limit: 一般 10 req / 5 min、admin 20 req / 5 min
- Log redaction: signed URL 削除、ID hash 化、user_agent redact

**§16 新章**: 改修サマリ + GitHub 復旧後アクションを集約

**見積更新**: 1.25d → 1.5d（+0.25d セキュリティ改修）

## ブランチ状態

| ブランチ | commit (local) | push 状態 |
|---|---|---|
| feature/bud-phase-0-auth | 81eb0cb（PR #55 fix） | 🚫 push 不可（GitHub suspended） |
| feature/bud-phase-d-specs-batch17-auto-fixes | d1483ac（PR #74 fix） | 🚫 push 不可（GitHub suspended） |

両 commit は origin に未反映。GitHub 復旧後 a-main がまとめて push 予定。

## GitHub 復旧後のアクション

### PR #55
1. `git push origin feature/bud-phase-0-auth`
2. a-bloom / a-review コメントとの突き合わせ
3. 認識相違あれば追加 commit（特に B1-B4 の解釈）
4. 東海林さん SQL 適用待ち（`bud-rls-v3-review-fixes.sql`）

### PR #74
1. `git push origin feature/bud-phase-d-specs-batch17-auto-fixes`
2. a-review コメントとの突き合わせ（5 件 #1-#5）
3. **#1 PDF PW 設計の東海林さん最終判断**（A 案 / B 案 / C 案）
4. 判断確定後、§6.5 + A-07 spec 連動更新（cash-payment-undecided.md §3/§4 + B-03/B-04）

## 注意事項遵守

- ✅ main / develop 直 push なし（push 自体できない）
- ✅ Supabase 本番への書込なし
- ✅ ファイル削除なし（rm 使用は /tmp の作業中ファイルのみ、git 管理外）
- ✅ 動作変更なしの範囲（B1-B4 は RLS 強化、TS は deprecation 注釈のみ）
- ✅ A-08 / A-07 本実装着手なし

## 関連ファイル

### PR #55 修正で触ったファイル
- `scripts/bud-rls-v3-review-fixes.sql`（新規）
- `src/app/bud/_lib/transfer-mutations.ts`（@deprecated 注釈追加）

### PR #74 修正で触ったファイル
- `docs/specs/2026-04-25-bud-phase-d-04-statement-distribution.md`（§6.3 / §6.4 / §6.5 / §6.6 / §15 / §16 改訂）

### effort-tracking.md
- 2 行追加（PR #55 修正 + PR #74 修正）

## 次セッションでの最初のアクション

1. GitHub サポート復旧確認（a-main から通知 or `gh api user`）
2. 復旧していたら両 commit を push
3. a-review コメント本文を取得し、自己分析との突き合わせ
4. PR #74 #1 の東海林さん判断を仰ぐ（A 案 / B 案 / C 案）
5. 判断確定後、A-07 spec / B-03 / B-04 連動更新

## 不明点・リスク

### 自己分析の限界
a-review コメント本文を直接読めないため、5 件の指摘が私の特定と一致するか未確認。GitHub 復旧後の突き合わせで認識相違が出る可能性あり。

### 東海林さん判断ペンディング
PR #74 #1 PDF PW 設計は A-07 採択結果（方式 2 = MMDD）の根本見直しを含むため、東海林さんの最終判断なしには確定できない。判断は GitHub 復旧後でも遅延しない（実装着手は Phase B = 2026-07 想定）。

### B-03 / A-07 spec 連動更新の保留
PR #74 #1 が確定するまで、B-03 / A-07 spec の PW 規則記述は現状維持。確定後にまとめて更新予定。
