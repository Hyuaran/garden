# フォルダ引越し前ハンドオフ — 2026-04-22

## セッション情報
- Branch: `feature/tree-phase-b`（直近コミット: `e168896` Phase A-FMK1 FileMaker風キーボードショートカット）
- 作業モジュール: Garden-Leaf 関電業務委託
- 引越し元: `C:\Users\shoji\Desktop\garden`
- 引越し先: `C:\garden\a-main`

## 今やっていること
Garden-Leaf Phase A のUI実装（新規案件登録・ステータス進行・FileMaker風ショートカット）。直近コミット `e168896` を `feature/leaf-kanden-supabase-connect` に cherry-pick or rebase で移すつもりだったが、別セッションとのブランチ競合で `feature/tree-phase-b` に紛れ込んでいる状態。

## 次にやること
1. 引越し後 `C:\garden\a-main` にて `feature/leaf-kanden-supabase-connect` に `e168896` を cherry-pick → push
2. Phase A-1c（添付ファイルアップロード・Supabase Storage 連携）に着手
3. 案件ID命名規則 `GLE0000001` 形式への移行（`project_garden_id_convention.md` 参照）

## 注意点
- **別セッションが同じワーキングツリーで動作**しており、ブランチが勝手に切り替わる現象が頻発。引越し後は同時起動を避けるか、ブランチごとに worktree を分離すること。
- `soil_kanden_cases` のテストデータ `K-20260422-101/102/103` は Phase A-2 テスト後に `ordered` 状態に戻してクリーンな状態。残してOK。
- 開発用仮RLSポリシー（`dev_cases_*`）が Supabase に適用中。本番前に削除必要（`supabase_dev_policies_20260422.sql` 参照）。
- Supabase service_role キーを PowerShell から使うとブラウザ判定で弾かれる。UA を `curl/8.4.0` に変えれば通る。
- Vercel は Pro プラン（$20/月、東海林承認済み）でチーム運用中。

## 直近の主要コミット
- `e168896` Phase A-FMK1 FileMaker風キーボードショートカット（Ctrl+F/N/↑/↓、Esc）
- `f8308fa` Phase A-2 ステータス進行UI（StatusAdvanceBar）
- `e01603e` Phase A-1b 新規案件モーダル+営業自動補完
- `457e0e3` Leaf Supabase接続版初期実装

## Supabase状態
- kanden_calendar: 216件投入済み（2026年度実カレンダー）
- soil_kanden_cases: 14件（サンプル10件 + Claudeテスト3件 + 東海林テスト1件）
- leaf_kanden_attachments / soil_supply_points: 空
- RLS: dev ポリシー適用中（認証済みなら全件閲覧・編集可）

## Phase完了状況
- ✅ Supabase接続
- ✅ 案件一覧表示・ステータス絞り込み・検索・詳細モーダル
- ✅ ロック画面（パスワードのみ）
- ✅ Phase A-1a 新規案件モーダル（最小構成）
- ✅ Phase A-1b 営業自動補完・取得区分アイコン・案件種別自動判定
- ✅ Phase A-2 ステータス進行UI（次へ進める・戻す・スキップ対応）
- ✅ Phase A-FMK1 FileMaker風キーボードショートカット
- ⏳ Phase A-1c 添付ファイルアップロード（次の実装対象）
- ⏳ 案件ID移行（K-形式 → GLE形式）
- ⏳ FileMaker風検索演算子（==、=、*、>、...、!、//）

## 参照ドキュメント
- 設計: `015_Gardenシリーズ/04_Garden-Leaf/001_関西電力業務委託/DB設計_取引マスタ_v2_20260420.md`
- マイグレーション: `015_Gardenシリーズ/04_Garden-Leaf/001_関西電力業務委託/supabase_migration_v1_20260420.sql`
- 仮RLS: `015_Gardenシリーズ/04_Garden-Leaf/001_関西電力業務委託/supabase_dev_policies_20260422.sql`
- PoC HTML: `garden-leaf-kanden-backoffice-v8_20260421120000.html`, `garden-leaf-kanden-input-v10_20260420160000.html`

## 引き継ぎメモ
引越し後、作業再開コマンド例:
```powershell
cd C:\garden\a-main
git status
git log --oneline -10
# feature/tree-phase-b にいるなら leaf ブランチに戻す
git checkout feature/leaf-kanden-supabase-connect
git cherry-pick e168896
git push origin feature/leaf-kanden-supabase-connect
```

---

# ⭐ 別セッション（Tree担当）追記 — 2026-04-22 Phase B MVP途中

## Tree セッション情報
- 担当モジュール: Garden-Tree
- 実装フェーズ: Phase B Step 1-3（Sprout 画面の結果を `soil_call_history` に保存する MVP）
- 作業ブランチ: `feature/tree-phase-b`（develop `6a30ccc` から派生）

## Tree セッションの今やっていたこと
`feature/tree-phase-b` ブランチで Garden-Tree の Sprout 架電画面の結果ボタン押下で `soil_call_history` に INSERT する MVP を実装中。`wip` commit で退避する。

## Tree セッションの完了済（コミット済）
- Phase A（認証・アカウント発行・ログイン）→ PR #4 で develop にマージ済（2026-04-22）
- `scripts/root-auth-schema.sql` / `scripts/root-rls-employees.sql` / `scripts/seed-tree-accounts.ts` など develop にマージ済

## Tree セッションの完了済（Supabase 側 / ローカル未コミット）
- **Supabase SQL Editor で `soil_call_history` テーブル・4 RLS ポリシー・updated_at トリガを適用済み**
  （SQL Editor 内の保存タブ名「通話履歴（Soil）管理テーブル」で確認可）
  - テーブル構造: `call_id (uuid) / employee_id / started_at / ended_at / duration_sec (generated) / result_code / result_category (generated) / call_mode / toss_to_id / list_name / customer_name / phone / relationship / next_contact_at / ng_reason / memo / created_at / updated_at`
  - RLS: 自分のコールのみ INSERT/SELECT、manager+ は全件 SELECT、admin+ は UPDATE

## Tree セッションの未コミット差分（本ハンドオフで wip コミット対象）
- `src/app/tree/_lib/queries.ts` — `insertCall()` 関数と `InsertCallParams` / `CallMode` 型を追加
- `src/app/tree/calling/sprout/page.tsx` — 結果ボタン押下で `insertCall` 呼び出し、保存中 / エラー表示UI
  - `connectedAt` / `hangupAt` / `savingCall` / `saveError` ステートを追加
  - `handleConnect` / `handleHangup` で時刻を記録
  - `handleSubmit` を async 化し、INSERT 成功時にリセット、失敗時にエラー表示

## Tree セッションの次にやること

1. 引越し後 `C:\garden\a-main` にて `feature/tree-phase-b` を checkout
2. dev server 起動（localhost:3000）→ `/tree/calling/sprout` で動作確認
   - テストログイン例: 社員番号 `0008` / パスワード（Forest用既存） で super_admin
   - 「通話接続（デモ）」→「切電」→ 結果選択（例：不通）→「次の架電へ」→ 保存成功を確認
   - Supabase Dashboard → Table Editor → `soil_call_history` に1件追加されていることを確認
3. OK なら wip コミットを amend で clean に整形 or 新規コミット
4. push → PR `feature/tree-phase-b` → develop 作成
5. 続いて Phase B Step 4〜6（`/tree/calling/branch` / `/tree/call`（通常/前確/後確）/ `/tree/follow-call`）に同様の保存連携を追加

## Tree セッションの注意点

- **`soil_call_history` は既に Supabase に存在**。SQL を再実行する必要なし。DROP も禁止（監査要件）
- **`insertCall` の戻り値** は `{ success: boolean, callId?: string, error?: string }`。保存失敗してもUI継続できる設計
- **通話時刻の扱い**: 未接続（「切電」を直接押した場合）は `started_at = ended_at` として 0秒扱いで保存される（「不通」結果と組み合わせて 有効率計算の分母に加算される）
- **RLS制約**: `insertCall` は `employee_id = 自分の employee_id` 以外は拒否される（RLS ポリシーによる）。デモ的に他人の employee_id で INSERT すると失敗するので注意
- **`cu` 変数** は現在 `DEMO_CUSTOMER`（山田太郎）を使用。本番では実顧客データに差替予定（Phase B 後半）
- **トス先クローザー（`toss_to_id`）** は現状 UI で指定する機能なし。Sprout → トス成功時に「どのクローザーに渡したか」を選択するUIは Phase B 後半で追加予定

## Tree セッションの推奨 clean commit メッセージ

```
feat(tree): Phase B Step 1-3 Sprout 架電画面を soil_call_history に保存

- scripts/soil-call-history.sql 相当を Supabase に適用済（テーブル + 4 RLS + トリガ）
- _lib/queries.ts: insertCall / InsertCallParams / CallMode 型を追加
- calling/sprout/page.tsx: 結果ボタン押下で insertCall、保存中 / エラー表示
- RLS: 自分のコールのみ INSERT/SELECT、manager+ は全件 SELECT、admin+ は UPDATE

次ステップ: branch / call / follow-call 画面にも同様の保存連携を追加
```

## Tree セッション Todo リスト（引継ぎ用）

| 状態 | 内容 |
|---|---|
| ✅ 完了 | Phase A 認証・アカウント発行（PR #4 マージ済） |
| ✅ 完了 | Phase B Step 0: feature/tree-phase-b ブランチ作成 |
| ✅ 完了 | Phase B Step 1: soil_call_history SQL 適用 |
| ✅ 完了 | Phase B Step 2: insertCall 実装（未コミット） |
| ✅ 完了 | Phase B Step 3: Sprout 画面保存連携（未コミット） |
| ⏳ 次 | Phase B Step 3 動作確認・commit・PR作成 |
| ⏳ 次 | Phase B Step 4: Branch 画面保存連携 |
| ⏳ 次 | Phase B Step 5: call 画面（通常/前確/後確）保存連携 |
| ⏳ 次 | Phase B Step 6: follow-call 画面保存連携 |
| ⏳ 後続 | Phase B-β: 初回ログイン時の誕生日入力画面 |
| ⏳ 後続 | Phase C: tree_daily_stats ビュー + KPI実データ接続 |
| ⏳ 後続 | Phase C+: Tree「＋新規アカウント追加」UI (KoT連携) |
| ⏳ 後続 | Phase D: FileMaker UX を Q&A 等8画面に展開 |
| ⏳ 別回 | アルバイト（トス/クローザー）+ CS（田中・南薗・森）アカウント発行 |

## Tree セッションが無視してよい差分

- `.worktrees/`（git管理外、引越し先にコピー不要）
- `src/app/tree/breeze/page (1).tsx`（別セッションの誤保存。削除してOK）

## Tree セッションの参照ドキュメント

- 設計書: `G:\マイドライブ\17_システム構築\07_Claude\01_東海林美琴\015_Gardenシリーズ\03_Garden-Tree\設計_Tree-Supabase連携_20260421.md`
- 実装プラン: 同フォルダ `実装プラン_PhaseA_認証_20260421.md`（Phase A 分）
- コール履歴サンプル: 同フォルダ `コール履歴_20260421.xlsx`（現行システム 807件）
- 親 CLAUDE.md: `015_Gardenシリーズ/CLAUDE.md`（ロール定義・アカウント一覧）
- Tree CLAUDE.md: `015_Gardenシリーズ/03_Garden-Tree/CLAUDE.md`（打刻機能・アカウント追加UI方針）

---

# ⭐ 別セッション（Root Auth担当）追記 — 2026-04-22 引越し前

## Root セッション情報
- 担当モジュール: Garden-Root（認証UI Phase 1）
- 作業ブランチ想定: `feature/root-auth`

## Root セッションの状況
引越し作業時にシェルが `feature/tree-phase-b` に居る状態で介入したため、このブランチには Root Auth の未コミット変更はなし。Root Auth Phase 1 UI は先のセッションで書きかけたが、ブランチ切替中に作業ツリーから消失しており、`feature/root-auth` ブランチには SQL スキーマ（`scripts/root-auth-schema.sql`）のみコミットされている状態。

## Root セッションの次にやること（引越し後）
- Tree 側 Phase A の成果（`develop` マージ済み）により、**Root 認証 UI は実質的に Tree の仕組みを流用すれば良い**状態。
  - `src/app/tree/_lib/auth.ts` と同パターンで `src/app/root/_lib/auth.ts` を作成
  - `src/app/tree/_state/TreeStateContext.tsx` と同パターンで `RootStateContext.tsx` を作成
  - アクセス判定は `ROOT_VIEW_ROLES`（manager 以上）／`ROOT_WRITE_ROLES`（admin 以上）を `src/app/root/_constants/types.ts` から参照
- 必要ファイル（引越し後、`develop` から分岐して新規ブランチ `feature/root-auth-ui` で作業推奨）:
  1. `_lib/auth.ts` / `_lib/audit.ts` / `_lib/session-timer.ts`
  2. `_state/RootStateContext.tsx`
  3. `_components/RootGate.tsx`
  4. `login/page.tsx`
  5. `layout.tsx`（RootStateProvider 追加）と `_components/RootShell.tsx`（認証チェック＋ログアウトボタン）を更新

## Root セッションの注意点
- 旧 `feature/root-auth` ブランチの SQL (`scripts/root-auth-schema.sql`) は `feature/tree-backend` 経由で develop にマージ済み。**別 SQL として再適用する必要なし**。
- 7段階ロール（toss/closer/cs/staff/manager/admin/super_admin）に更新済み。Root アクセスは `ROOT_VIEW_ROLES` を参照すること。

---

# ⭐ 別セッション（Bud Phase 0担当）追記 — 2026-04-22 引越し前

## Bud セッション情報
- 担当モジュール: Garden-Bud（Phase 0 認証基盤）
- 作業方式: superpowers:subagent-driven-development（writing-plans で計画 → タスクごとに subagent 実装 → spec/quality 両レビュー）
- worktree: `C:\Users\shoji\Desktop\garden\.worktrees\feature-bud-phase-0-auth`
- 作業ブランチ: `feature/bud-phase-0-auth`（develop から分岐）

## Bud セッションの完了済（コミット済）
- Task 1: ブランチ作成（worktree 経由）
- Task 2: Root 前提 SQL `scripts/root-prerequisites.sql`
  - root_employees に birthday / garden_role / user_id カラム追加
  - **7段階ロール対応**（CHECK制約: toss/closer/cs/staff/manager/admin/super_admin）
  - コミット: `9a6fc36` → `cf4535a`（7段階修正）
- Task 3: Bud スキーマ SQL `scripts/bud-schema.sql`
  - 5テーブル（bud_users / bud_transfers / bud_statements / bud_salary_batches / bud_salary_details）
  - updated_at トリガ4本 + インデックス9本 + RLS有効化5テーブル
  - コミット: `e806d8f`
- 仕様書類の7段階ロール整合性修正:
  - v2 設計書（`feature/bud-design` ブランチ `cb9c7c9`）
  - 親 CLAUDE.md `015_Gardenシリーズ/CLAUDE.md`（§9の「5段階」残存を修正）
  - Phase 0 実装計画 `docs/superpowers/plans/2026-04-20-bud-phase-0-auth.md`（worktree `21cc6bb`）

## Bud セッションの未コミット差分
**なし**（worktree は clean state）

## Bud セッションの次にやること（引越し後）

引越し後、`C:\garden\a-main\.worktrees\feature-bud-phase-0-auth\` で継続：

1. **Task 3 の spec review + code quality review**（subagent 経由で dispatch 予定だったが引越しで中断）
2. **Task 4**: Bud RLS ポリシー SQL `scripts/bud-rls.sql` 作成
   - `bud_has_access()` / `bud_is_approver_or_above()` ヘルパー関数
   - 各テーブルの RLS ポリシー（SELECT/INSERT/UPDATE）
3. **Task 5**: 初期ユーザー登録 + `scripts/seed-bud.sql`
   - 東海林: Supabase Auth で `emp0008@garden.internal` 作成 → UUID を `root_employees.user_id` に紐付け → `bud_users` に admin として INSERT
4. **Task 6-13**: フロントエンド実装
   - TypeScript 型定義 / Supabase クライアント / 認証ヘルパー / BudStateContext / BudGate / BudShell / ログイン画面 / layout / dashboard
   - 詳細は `docs/superpowers/plans/2026-04-20-bud-phase-0-auth.md` 参照
5. **Task 14**: 動作確認（`/bud/login` → `/bud/dashboard`、スマホ表示、アクセス拒否）
6. **Task 15**: PR 作成

## Bud セッションの注意点

- **7段階ロール (2026-04-21改訂) で統一済み**。次セッションで implementer subagent を dispatch する際も、`toss / closer / cs / staff / manager / admin / super_admin` の7値を使うこと。
- **SQL は Supabase Dashboard への手動適用が必要**。Task 2（root-prerequisites.sql）と Task 3（bud-schema.sql）はまだ Dashboard で実行されていない。実装再開前に東海林に SQL Editor で順次実行してもらう。
- **code-quality-reviewer で Critical 検出経験あり**: 5段階→7段階の矛盾を code-quality-reviewer subagent が検出してくれた。メモリ `feedback_garden_pre_build_check` と `project_garden_auth_policy` の参照を怠らないこと。
- **subagent dispatch 前のチェック**: plan の記述に 5段階が残っていないか最終確認する。`Grep "5段階|'toss','closer','manager'"` で引っかからなければOK。
- **worktree の運用**: `.worktrees/feature-bud-phase-0-auth` には `node_modules` 含めインストール済み。引越し後 `npm install` をやり直さなくても動くはず（Node.jsバージョン互換性に依存）。動かなかったら再 `npm install`。

## Bud セッションの Todo リスト（引継ぎ用）

| 状態 | 内容 |
|---|---|
| ✅ 完了 | Task 1: ブランチ作成 |
| ✅ 完了 | Task 2: Root 前提 SQL（7段階修正含む） |
| ✅ 完了 | Task 3: Bud スキーマ SQL |
| ✅ 完了 | 仕様書類の7段階整合性修正（v2 spec / CLAUDE.md / plan） |
| ⏳ 次 | Task 3 の spec/quality review |
| ⏳ 次 | Task 4: Bud RLS ポリシー SQL |
| ⏳ 次 | Task 5: 初期ユーザー + seed SQL |
| ⏳ 次 | Task 6-13: フロントエンド実装 |
| ⏳ 次 | Task 14: 動作確認 |
| ⏳ 次 | Task 15: PR 作成 |

## Bud セッションの参照ドキュメント

- 実装計画: `.worktrees/feature-bud-phase-0-auth/docs/superpowers/plans/2026-04-20-bud-phase-0-auth.md`
- v2 設計書: `feature/bud-design` ブランチ `docs/superpowers/specs/2026-04-17-bud-design-v2.md`
- 親 CLAUDE.md（7段階ロール詳細）: `015_Gardenシリーズ/CLAUDE.md`（特に §4 認証ポリシー）
- 子 CLAUDE.md（Bud固有ルール）: `015_Gardenシリーズ/05_Garden-Bud/CLAUDE.md`
- 未決事項: `015_Gardenシリーズ/05_Garden-Bud/未決事項_20260419.md`（手渡し現金支給）
- 認証ポリシー（メモリ）: `project_garden_auth_policy` — **7段階ロール＋cs以上で前確/後確画面閲覧可**
- 作業再開時の必読メモリ: `feedback_garden_pre_build_check`（実装前＋作業中の定期メモリ確認ルール）

## Bud セッションの subagent 作業再開時の dispatch テンプレ

引越し後、次セッションで superpowers:subagent-driven-development を再読み込みして、以下の順で dispatch:

1. spec reviewer for Task 3（bud-schema.sql の line-by-line verification）
2. code-quality reviewer for Task 3
3. implementer for Task 4（bud-rls.sql 作成）
...

各 subagent への prompt は plan ファイル `2026-04-20-bud-phase-0-auth.md` から該当 Task の full text をコピーして渡す。FULL TEXT を渡す（plan を読ませるのではない）。
