# Handoff — 2026-04-22 (担当: a-leaf / 東海林A)

## 今やっていること
Garden-Leaf 関電業務委託 Phase A のUI実装。Supabase接続→新規案件登録→ステータス進行→FileMaker風UXまで進行。Phase A-FMK1（キーボードショートカット）まで実装したが **push失敗で消失**、再実装待ち。

## 次にやるべきこと

### 最優先: Phase A-FMK1 の再実装（約10分、コード内容は既知）
`src/app/leaf/backoffice/page.tsx` に以下を追加：

1. `import { useRef } from "react";` 追加
2. `searchInputRef = useRef<HTMLInputElement>(null)` を state 近くに追加
3. `navigateCase(dir: 1|-1)` useCallback で filtered 配列内の前後移動
4. `useEffect` でグローバル keydown リスナ:
   - **Ctrl+F** → `searchInputRef.current?.focus(); select()`
   - **Ctrl+N** → `setShowNewCase(true)` (既開なら何もしない)
   - **Ctrl+↑/↓** → `selected` があれば `navigateCase(-1|1)`
   - **Esc** → `selected` があれば `setSelected(null)`
5. 検索 input に `ref={searchInputRef}` + `onKeyDown` で Esc → `setSearch(""); blur()`
6. `+新規案件` ボタンに `title="新規案件登録 (Ctrl+N)"`
7. `CaseDetailModal` に `filteredList: KandenCase[]` と `onNavigate: (dir) => void` を追加
8. モーダルヘッダーに ▲`(currentIdx+1)/(totalCount)`▼ の前後ボタン + 位置表示
   - 先頭/末尾では disabled
   - title に Ctrl+↑/↓ ヒント
9. page.tsx 側で `onNavigate={navigateCase}` `filteredList={filtered}` を渡す

### その後の順序
1. Phase A-1c: 添付ファイルアップロード（電灯/動力/ガス/諸元/受領書 + Supabase Storage）
2. 案件ID命名規則 `GLE0000001` 形式への移行
3. FileMaker風検索演算子（`==`, `=`, `*`, `>`, `...`, `!`, `//`）

## 注意点・詰まっている点

### 🚨 重要
- **`feature/tree-phase-b` の remote にある `87eb055` 以降のコミットは他セッション（Tree/Forest/Bud）の作業**。Leaf には無関係。
- **Phase A-FMK1 コミット `e168896` は remote に存在しない**（push されず消失）。再実装必須。
- **旧フォルダ `C:\Users\shoji\Desktop\garden` は引越し済み**（恐らく削除または `C:\garden\a-main` へ移動）。そこからの救出は不可能と想定。

### 開発中の暫定対応
- Supabase の RLS は開発用仮ポリシー (`dev_cases_*` on `soil_kanden_cases` / `leaf_kanden_attachments` 等) が適用中。本番前に削除必要。
- `company_id` は NewCaseModal で `COMP-001` 固定（root_employees から取得できなければ）。本番では所属企業を正確に引く必要あり。
- `case_id` 形式は現状 `K-YYYYMMDD-NNN`。新命名規則 `GLE0000001` への移行が未着手。
- Supabase service_role キーを PowerShell からテスト使用する際、`User-Agent: curl/8.4.0` を指定しないとブラウザ判定で401。

### テストデータ（残しておいてOK）
- `K-20260422-101` Claudeテスト①（latest）
- `K-20260422-102` Claudeテスト②（replaced・旧PDあり）
- `K-20260422-103` Claudeテスト③（outside・至急SW）
- すべて Phase A-2 テスト後に `ordered` 状態に戻してクリーン

## 関連情報

### ブランチ
- **作業ブランチ**: `feature/leaf-kanden-supabase-connect`
- **ベース**: `develop`
- **注意**: `feature/tree-phase-b` には触らない（他セッション担当）

### 関連ファイル
- `src/app/leaf/backoffice/page.tsx` — 一覧＋詳細モーダル（ここに Phase A-FMK1 を再追加）
- `src/app/leaf/backoffice/_components/NewCaseModal.tsx` — 新規案件登録モーダル
- `src/app/leaf/backoffice/_components/StatusAdvanceBar.tsx` — ステータス進行バー
- `src/app/leaf/backoffice/_components/StatusBadge.tsx` / `StatusFlow.tsx` / `SupplyInline.tsx` — 表示パーツ
- `src/app/leaf/_lib/queries.ts` — Supabaseクエリ（`fetchEmployeeByNumber` / `createCase` / `updateCaseStatus` / `generateCaseId` 等）
- `src/app/leaf/_lib/auth.ts` — 5分ロック・ログイン
- `src/app/leaf/_lib/calendar.ts` — 2026年度実カレンダー（18コード×12ヶ月）
- `src/app/leaf/_constants/colors.ts` — カラーパレット
- `src/app/leaf/_lib/types.ts` — KandenCase / KandenStatus / STATUS_FLOW など

### 関連ドキュメント（Gドライブ）
- `015_Gardenシリーズ/04_Garden-Leaf/001_関西電力業務委託/`
  - `DB設計_取引マスタ_v2_20260420.md` — DB設計
  - `supabase_migration_v1_20260420.sql` — マイグレーション
  - `supabase_dev_policies_20260422.sql` — 開発用仮RLS
  - `garden-leaf-kanden-backoffice-v8_20260421120000.html` — Backoffice PoC
  - `garden-leaf-kanden-input-v10_20260420160000.html` — Input PoC
  - `index.html` — 成果物ポータル

### 関連メモリ（MEMORY.md index）
- `project_kanden_workflow` — 8段階ステータス・業務ルール
- `project_backoffice_search_spec` — 検索仕様
- `project_garden_filemaker_ux` — FileMaker風UX（再実装対象の指針）
- `project_garden_id_convention` — GXX0000001 命名規則
- `project_garden_auth_policy` — 認証・7段階ロール
- `feedback_input_field_styling` — input/textarea に color+background 必須
- `project_garden_dev_env` — GitHub/Vercel/Supabase構成

### Supabase状態
- URL: `https://hhazivjdfsybupwlepja.supabase.co`
- テーブル: `soil_kanden_cases` (14件), `kanden_calendar` (216件), `leaf_kanden_attachments` (0件), `soil_supply_points` (0件)
- 仮RLS適用中（認証済みなら全許可）
- Auth登録済: `emp0000@garden.internal` (後道), `emp0008@garden.internal` (東海林)

## 継続済みコミット（remote確認済み）
- `f8308fa` feat(leaf): Phase A-2 ステータス進行UI（1クリック次へ進める + 戻す）
- `e01603e` feat(leaf): Phase A-1b 新規案件登録モーダル+営業自動補完+UI改善
- `457e0e3` feat(leaf): Garden-Leaf 関電業務委託モジュール Supabase接続版を実装

## セッションBへの引き継ぎコマンド
```powershell
cd C:\garden\a-leaf
git fetch --all
git checkout feature/leaf-kanden-supabase-connect
git pull
# Phase A-FMK1 再実装 → commit → push
# その後 Phase A-1c（添付ファイル）に着手
```
