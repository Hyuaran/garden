# Handoff - Garden-Leaf a-main 006 Kintone 解析 #22 反映（経費 type / business_id 設計指針）

- 開始時刻: 2026-04-26 00:25 JST 頃
- 終了時刻: 2026-04-26 00:30 JST 頃
- セッション: a-leaf
- 状態: **ローカル commit のみ、push は GitHub 復旧待ち**（チケット #4325863）
- 前 handoff: `docs/handoff-leaf-202604260010.md`（a-review #65 セキュリティ修正）

## 反映指示の出所

- **a-main 006 Kintone 解析バッチ**: `C:\garden\_shared\decisions\decisions-kintone-batch-20260426-a-main-006.md` §3.5
- 該当 #22: **App 95（経費一覧）他事業展開**
- 確定内容: 当面関電専用、新事業展開時に列追加なしで拡張可能設計（type / business_id 列）
- Leaf 担当範囲: 「将来事業展開時に対応可能な設計を Leaf 拡張 spec に記載」（優先度 🟢 低）

## 反映方針の判断

decisions §3.5 が指す「Leaf 拡張 spec」は現状実在しない（Leaf 配下 spec は A-1c v3 のみ）。a-main 指示「Leaf 関電 経費関連 spec」も同様に未存在。

選択した対応:
- **新規 spec ファイル**を起票し、Leaf 全体の拡張可能設計指針として集約
- 経費テーブル（`bud_expenses`）は **Bud 管轄**だが、business_id / type 列パターン + RLS は Leaf v3 で確立済の `leaf_user_in_business()` 関数を再利用する形で記載
- A-1c v3（添付ファイル）のスコープ汚染を回避するため、A-1c spec への追記ではなく独立 spec とした

## 成果物

### 新規 spec ファイル

- **`docs/superpowers/specs/2026-04-26-leaf-future-extensions-design.md`**（241 行、新規）

7 sections:
1. 目的（a-main 006 #22 反映の経緯、A-1c v3 が先行実装例）
2. 拡張可能設計の原則（business_id / type 必須 2 列、leaf_businesses FK、leaf_user_in_business RLS）
3. **経費テーブル設計指針**（bud_expenses + business_id 列、Phase B-1 で a-bud / a-leaf 連携起草、移行戦略）
4. 既存 Leaf テーブル状況（A-1c v3 で導入済 `leaf_businesses` / `leaf_user_businesses` / `leaf_kanden_attachments` の整理）
5. 関連 Garden 全体パターン（root_settings / 申請承認 / 削除パターン）
6. 実装スケジュール（Phase B-1 以降、A-1c 完走後）
7. 改訂履歴

### 主な確定方針

- **テーブル名**: `bud_expenses_kanden`（事業 prefix）❌ → `bud_expenses` + `business_id` 列 ✅
- **business_id 値**: 当面 `'kanden'` 固定、将来 `'sb'` / `'docomo'` / `'sonet'` 等を行追加で対応
- **RLS**: A-1c v3 と同パターン（`leaf_user_in_business(business_id) OR garden_role_of(...) IN ('admin', 'super_admin')`）
- **削除パターン**: `deleted_at` / `deleted_by` 列、論理削除全員可 + 物理削除 admin+（A-1c v3 と統一）
- **Phase B-1 起草担当**: a-bud（spec 本体）+ a-leaf（UI / 集計 view）

## ブランチ + commit

- ブランチ: `feature/leaf-future-extensions-spec`（develop ベース）
- 含む commit:
  - `fd1e02e`: 27e31c0 cherry-pick（npm install lockfile）
  - `<新>`: 本 spec 起票 + handoff + effort-tracking 追記

## ファイル変更一覧

| ファイル | 変更 |
|---|---|
| `docs/superpowers/specs/2026-04-26-leaf-future-extensions-design.md` | 新規 (241 行) |
| `docs/handoff-leaf-202604260030.md` | 新規（本ファイル）|
| `docs/effort-tracking.md` | +1 行（Leaf 将来拡張設計指針 spec 起草）|

## 次のアクション（GitHub 復旧後）

1. `feature/leaf-future-extensions-spec` を push
2. PR 発行（base: develop / レビュアー: a-bloom）
3. PR タイトル: `docs(leaf): 将来拡張 設計指針 spec (a-main 006 Kintone #22 反映)`
4. a-bloom レビュー → develop merge
5. Phase B-1 着手時に a-bud / a-leaf が本 spec を参照して bud_expenses 起草

## 並行する待機 PR / commit（push 待ち）

| 内容 | ブランチ | 状態 |
|---|---|---|
| a-review #65 セキュリティ修正 | `feature/leaf-a1c-task-d1-pr` | local commit `4247005` + `c44dc0e` 待機 |
| 本 spec 起票 | `feature/leaf-future-extensions-spec` | local commit 待機 |

## 関連

- decisions: `C:\garden\_shared\decisions\decisions-kintone-batch-20260426-a-main-006.md`
- 関連 Kintone アプリ: App 95（経費一覧、26 fields）
- 関連 memory: `feedback_kintone_app_reference_format.md` / `project_garden_change_request_pattern.md` / `project_delete_pattern_garden_wide.md`

## 関連 handoff

- `docs/handoff-leaf-202604251700.md` 〜 `docs/handoff-leaf-202604252315.md`（前 round 群）
- `docs/handoff-leaf-202604260010.md`（a-review #65 セキュリティ修正）
- **`docs/handoff-leaf-202604260030.md`**（本ファイル）
