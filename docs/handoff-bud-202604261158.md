# Handoff — 2026-04-26 11:58 / a-bud (A) PR #74 Y 案 + フォールバック改訂

## セッション概要

- **担当**: a-bud (A) / ブランチ `feature/bud-phase-d-specs-batch17-auto-fixes`
- **モード**: GitHub suspended 継続、ローカル commit のみ（push 不可）
- **対応**: PR #74 #1 の東海林さん最終判断「Y 案 + フォールバック」を spec 全体に反映
- **直前の状態**: commit `d1483ac` で a-review #1-#5 を改修済（5 件中 #1 のみ判断保留）

## 完了した作業

### 1. D-04 spec（`docs/specs/2026-04-25-bud-phase-d-04-statement-distribution.md`）

#### §2 採択結果セクション全面置換
- 旧「方式 2 = MMDD 4 桁 PW」を「**Y 案 + フォールバック**」に書換
- §2.1 主経路: Tree マイページ
- §2.2 通常フロー: メール DL リンク（24h ワンタイム、PW なし PDF） + LINE Bot 通知
- §2.3 例外フロー: メール DL リンク + PW 保護 PDF（強ランダム 16 文字）+ マイページ PW 確認
- §2.4 配信戦略整理表（4 動線）
- §2.5 やらないこと（旧採択 MMDD / 社員番号下 4 桁を明示禁止）
- §2.6 現金手渡し受給者の扱い（既存内容を維持）

#### §6 配信フロー全面書換
- §6.0 設計方針（Y 案 + フォールバック）
- §6.1 通常フロー（LINE 友だちあり）— メール文面 + LINE Bot 文面
- §6.2 例外フロー（LINE 非友だち）— メール文面（PW 規則の平文記載なし）
- §6.3 LINE 友だち判定ロジック（`decideDeliveryMethod` 関数 + `line_friend_status` ENUM）
- §6.4 メール配信技術選定 + SPF/DKIM/DMARC + SMTP TLS + LINE Bot 認証
- §6.5 監査ログ（メール DL リンク + マイページ DL の Server Action 統合版）
- §6.6 配信失敗リトライ（1h/6h/24h、LINE 失敗時の自動フォールバック格上げ）
- §6.7 PW 保護 PDF（フォールバック専用、技術選定）
- §6.8 旧採択（方式 2 = MMDD）の破棄根拠（履歴）
- §6.10 旧 §6.6 ダウンロード Server Action（履歴）
- §6.11 Tree マイページ統合（既存内容を維持）

#### §8 テーブル定義改訂
- 旧 `bud_salary_notifications` を **`bud_payroll_notifications`** に改訂
  - `delivery_method` ENUM（line_email / fallback_email_pw / manual）
  - DL トークン列（dl_token、dl_token_expires_at、dl_used_at、dl_ip）
  - LINE Bot 列（line_status、line_user_id_hash、line_message_id、line_sent_at）
  - フォールバック PW 列（fallback_password_hash、fallback_password_plain_temp、fallback_password_displayed_at）
  - CHECK 制約: フォールバック時は PW 必須
- 環境変数の明記（LINE_OFFICIAL_CHANNEL_ACCESS_TOKEN、PAYROLL_LINK_EXPIRY_HOURS=24、PAYROLL_PDF_PASSWORD_LENGTH=16 等）

### 2. A-07 spec（`docs/specs/2026-04-24-bud-a-07-cash-payment-undecided.md`）

#### §14 新章追加（このブランチにはまだ採択結果未反映だったため）
- §14.1 5 論点採択結果テーブル（論点 3 が「方式 2'」に変更）
- §14.2 論点 3 の改訂採択（Y 案 + フォールバック詳細）
- §14.3 5 未確認事項 U1-U5（暫定推測で合意済）
- §14.4 方式 2' 実装ヒント（bcryptjs クライアント計算削除、サーバ側完結に統一）
- §14.5 Phase B 着手時 TODO（PW タイプ決定 → 削除、LINE Bot SDK 追加 等）
- §14.6 反映済みファイル一覧

### 3. Phase D 関連 spec ヘッダー更新

| spec | 更新内容 |
|---|---|
| D-02 §1.4 | A-07 採択 = Y 案 + フォールバック明記、配信ロジックは D-04 委譲 |
| D-03 §1.4 | 配信ロジックは D-04 流用（Y 案 + フォールバック）、`bud_payroll_notifications.bonus_record_id` 識別 |
| D-06 | A-07 直接参照なし、変更不要 |
| D-07 §1.4 | 振込実行と配信は独立、配信は D-04 委譲（Y 案 + フォールバック） |
| D-08 §3.3.5 | 配信エッジケーステストを Y 案 + フォールバック対応に書換（LINE 判定 / 通常 / 例外 / 監査） |

### 4. effort-tracking.md
- PR #74 D-04 Y 案 + フォールバック確定行を追加（実績 0.25d）

## ブランチ状態

| ブランチ | commit (local) | push 状態 |
|---|---|---|
| `feature/bud-phase-d-specs-batch17-auto-fixes` | `d1483ac`（a-review 5 件改修） + 本 commit | 🚫 push 不可（GitHub suspended） |

## GitHub 復旧後のアクション

1. 本ブランチを push（`git push origin feature/bud-phase-d-specs-batch17-auto-fixes`）
2. PR #74 のレビューコメントとの突き合わせ
3. 認識相違あれば追加 commit
4. マージ可否判断（東海林さん）
5. マージ後、A-07 採択改訂の Phase B 着手前リマインダー（2026-07 想定）

## 注意事項遵守

- ✅ main / develop 直 push なし（GitHub suspended で push 自体不可）
- ✅ Supabase 本番への書込なし
- ✅ ファイル削除なし
- ✅ 動作変更なし（spec 改訂のみ、コード変更なし）
- ✅ A-07 採択結果改訂は東海林さん確定後に着手（指示通り）

## 関連 memory（参照済み）

- `project_payslip_distribution_design.md` — 本日確定、Y 案 + フォールバック設計のソース
- `project_garden_login_office_only.md` — マイページ社内 PC 限定の前提
- `project_chatwork_bot_ownership.md` — Bot 運用ルール

## 次セッションでの最初のアクション

1. GitHub サポート復旧確認
2. 復旧していたら本ブランチ push + PR #74 状態確認
3. PR #55 ブランチ（feature/bud-phase-0-auth）の push と a-bloom レビュー再依頼
4. マージ作業の優先順位を a-main と相談
