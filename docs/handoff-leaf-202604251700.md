# Handoff - Garden-Leaf A-1c v3 実装着手 (a-leaf 自律実行モード開始)

- 開始時刻: 2026-04-25 17:00 JST
- セッション: a-leaf
- モード: イレギュラー自律実行（東海林さん外出のため、a-main 経由で発動）
- 稼働制限: 最大 3 時間（2026-04-25 20:00 まで）
- 作業ブランチ: `feature/leaf-a1c-impl`（develop ベース、spec/plan v3 取込済）

## 実行方針

a-main 自律実行モード指示（2026-04-25 16:50 頃）に従い、Phase 0 事前調査 → Subagent-Driven Development で 33 task を順次消化。判断保留が出たら即停止 + `docs/pause-YYYYMMDDHHMM-a-leaf.md` 起票。

## Phase 0 事前調査結果（自律判定済）

| 確認項目 | 結果 | 自律判定 |
|---|---|---|
| `root_settings` テーブル | develop 未存在（Tree spec で「Root 側で新設」予定だが未実装）| Leaf migration 内で `CREATE TABLE IF NOT EXISTS` 自走 |
| pgcrypto extension | Bloom が `bloom-schema.sql` で有効化済 | 念のため Leaf migration 冒頭で `CREATE EXTENSION IF NOT EXISTS` 自走 |
| Root A-3-g 関数 | `is_user_active()` / `garden_role_of(uid)` 完備、develop マージ済 | そのまま参照 |
| Root A-3-h | `kou_otsu` / `dependents_count` / `deleted_at`（root_employees）追加。Leaf 影響なし | 参照不要 |
| garden_role 実装 | CHECK 制約（PostgreSQL ENUM ではない）。spec v3 「enum」表記は厳密には CHECK 制約 | RLS / ロジック変更不要 |

## ブランチ戦略確定

- 新ブランチ `feature/leaf-a1c-impl`（develop ベース、コミット `f292a70`）作成済
- spec/plan v3 を 1 commit で取込（`feature/leaf-kanden-supabase-connect` から checkout 経由）
- 各 task は本ブランチ上で commit 積上、最終的に PR 1 本（or 段階別）を develop に発行
- `feature/leaf-kanden-supabase-connect` の spec/plan 履歴は別途 PR 化（後日 a-main 判断）
- main / develop 直接 push 禁止を厳守

## v3 改訂事項チェックリスト（実装中に漏れ防止）

- [ ] **論理削除全員可能化**（manager+ ガード撤廃、Garden 共通パターン、Task D.8 / A.3）
- [ ] **画像 DL 専用パスワード**（root_settings + bcrypt + RPC、super_admin 管理、Task D.1 / D.13 / A.3b / A.7）
- [ ] **変更履歴 trigger / log 列**（leaf_kanden_attachments_history、Task D.1）
- [ ] **Root マイページ DL PW 設定 UI**（Task A.7、新規）
- [ ] HEIC 文言 A+C 合成版
- [ ] UNDO 5 秒固定
- [ ] 削除済バッジ全員可視

## 着手順序（plan §5）

1. **Phase 0**: Task 0.2 (npm install) → 0.3 (vitest 設定)
2. **Phase D**: D.1 (migration v3) → D.2 → ... → D.14 (カバレッジ)
3. **Phase A**: A.1 (Card) → ... → A.6 (page 組込) → **A.7 (Root DL PW UI、v3 新規)**
4. **Phase B**: B.1 (CategoryPicker) → B.2 → B.3
5. **Phase F**: F.1 (手動 RLS) → ... → F.5 (最終 push)

## 停止条件（a-main 指示準拠）

- 設計判断・仕様解釈の幅発生
- 同じエラーに 3 回失敗 → 該当タスクスキップ
- 東海林さん確認事項発生
- v3 改訂事項の解釈が分かれる場合
- 3 時間経過（20:00 JST）

## 完了 / 停止時の出力

- `docs/handoff-leaf-YYYYMMDDHHMM.md` 完了版で更新
- `docs/effort-tracking.md` に Leaf A-1c 行追加
- 各 task 完了ごとに develop 向け PR 発行（一括 PR or 段階別、状況により判断）

## 関連ファイル

- spec: `docs/superpowers/specs/2026-04-23-leaf-a1c-attachment-design.md` (v3, 1637 行)
- plan: `docs/superpowers/plans/2026-04-25-leaf-a1c-attachment.md` (v3, 4407 行)
- Root A-3-g: `scripts/root-schema-patch-a3g.sql`（develop マージ済）
- Root A-3-h: `scripts/root-schema-patch-a3h.sql`（develop マージ済）

## 退避済 stash（前ブランチ）

`feature/leaf-kanden-supabase-connect` 上で stash 退避済:
- `.claude/settings.json` / `CLAUDE.md` (4/24 設定再配布分)
- `.claude/settings.local.json` / `tmp-specs/` (作業ファイル)

`stash@{0}: a1c-impl-setup: settings/CLAUDE.md/tmp-specs 退避`
