# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# worktrees
/.worktrees

# Claude Code ユーザーローカル設定
.claude/settings.local.json

# typescript
*.tsbuildinfo
next-env.d.ts

# Claude Code local overrides (per-machine / per-user)
.claude/settings.local.json

# a-audit script test outputs (再生成可能、commit 不要、main- No. 211 GO)
docs/audit-memory-index-coverage-result-*.json
docs/audit-memory-baseline-diff-result-*.json
docs/audit-dispatch-format-check-result-*.json
docs/audit-snapshot-integrity-result-*.json
docs/audit-migration-apply-status-result-*.json
