import { ReactNode } from "react";

export const metadata = {
  title: "Garden Leaf — 関電業務委託",
};

export default function LeafLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
