import {readFileSync} from "node:fs";import {join} from "node:path";import {describe,expect,it} from "vitest";const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260826000002_system_mypage_submissions.sql"),"utf8");describe("mypage submissions migration",()=>{it("uses the real text employee key and all workflow states",()=>{expect(sql).toContain("employee_id text not null references public.root_employees(employee_id)");for(const state of ['received','in_progress','amount_proposing','awaiting_employee','completed','confirmed','withdrawn'])expect(sql).toContain(`'${state}'`)});it("is server-only and has an updated-at trigger",()=>{expect(sql).toContain("revoke all on public.system_mypage_submissions from anon,authenticated");expect(sql).toContain("root_update_updated_at()")})});
