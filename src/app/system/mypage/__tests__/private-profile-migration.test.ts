import {readFileSync} from "node:fs";import {join} from "node:path";import {describe,expect,it} from "vitest";
const sql=readFileSync(join(process.cwd(),"supabase/migrations/20260826000001_mypage_employee_private_profile.sql"),"utf8");
describe("mypage private profile migration",()=>{it("adds per-employee commute amounts",()=>{expect(sql).toContain("commute_daily_allowance integer");expect(sql).toContain("commute_monthly_cap integer")});it("keeps my numbers service-role only without a select policy",()=>{expect(sql).toContain("create table if not exists public.root_employee_my_numbers");expect(sql).toContain("enable row level security");expect(sql).toContain("revoke all on table public.root_employee_my_numbers from public, anon, authenticated");expect(sql).toContain("grant all on table public.root_employee_my_numbers to service_role");expect(sql).not.toMatch(/create\s+policy[\s\S]*root_employee_my_numbers/i)});});
