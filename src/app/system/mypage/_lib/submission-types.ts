export const SUBMISSION_LABELS={emergency_contact:"緊急連絡先変更",commute_route:"通勤経路変更",bank_account:"給与受取口座の変更",resignation:"退職届",nda:"秘密保持誓約書"} as const;
export type SubmissionType=keyof typeof SUBMISSION_LABELS;
export type SubmissionStatus="received"|"in_progress"|"amount_proposing"|"awaiting_employee"|"completed"|"confirmed"|"withdrawn";
export type SubmissionRow={id:string;employee_id:string;submission_type:SubmissionType;payload:Record<string,unknown>;status:SubmissionStatus;proposed_one_way:number|null;kintone_status:string;kintone_note:string|null;created_at:string;root_employees?:{name?:string;kot_employee_id?:string|null;commute_monthly_cap?:number|null}|null};
export function validateSubmission(type:SubmissionType,payload:Record<string,unknown>){const required:Record<SubmissionType,string[]>={emergency_contact:["name","relationship","phone"],commute_route:["station","effectiveDate"],bank_account:["bankName","bankCode","branchName","branchCode","accountNumber","holderKana"],resignation:["desiredDate"],nda:["signature"]};if(!required[type]||required[type].some(key=>!String(payload[key]??"").trim()))return "必須項目を入力してください。";if(type==="bank_account"&&(!/^\d{4}$/.test(String(payload.bankCode))||!/^\d{3}$/.test(String(payload.branchCode))||!/^\d{1,8}$/.test(String(payload.accountNumber))))return "金融機関コード・支店コード・口座番号の桁数をご確認ください。";if(type==="nda"&&payload.agreed!==true)return "内容への同意が必要です。";return null;}
