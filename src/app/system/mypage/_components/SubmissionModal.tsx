"use client";import {useState,type FormEvent} from "react";import {SUBMISSION_LABELS,type SubmissionType} from "../_lib/submission-types";import styles from "../mypage.module.css";
const definitions:Record<SubmissionType,Array<[string,string,string,boolean?]>>={emergency_contact:[["name","氏名","text"],["relationship","続柄","text"],["phone","電話番号","tel"],["address","住所（任意）","text",true]],commute_route:[["station","新しい最寄り駅","text"],["effectiveDate","適用希望日","date"],["note","補足（任意）","textarea",true]],bank_account:[["bankName","銀行名","text"],["bankCode","金融機関コード（4桁）","text"],["branchName","支店名","text"],["branchCode","支店コード（3桁）","text"],["accountNumber","口座番号（8桁以内）","text"],["holderKana","口座名義カナ","text"]],resignation:[["desiredDate","退職希望日","date"],["reason","理由（任意）","textarea",true]],nda:[["signature","氏名（電子署名）","text"]]};
export default function SubmissionModal({type,onClose,onSent}:{type:SubmissionType;onClose:()=>void;onSent:()=>void}){const [values,setValues]=useState<Record<string,string|boolean>>({});const [error,setError]=useState("");const [sending,setSending]=useState(false);async function submit(event:FormEvent){event.preventDefault();setSending(true);setError("");const response=await fetch("/api/system/mypage/submissions",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({type,payload:values})});const result=await response.json();setSending(false);if(!response.ok)return setError(result.error||"送信できませんでした。");onSent();}return <div className={styles.modalBackdrop} role="presentation" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><section className={styles.submissionModal} role="dialog" aria-modal="true" aria-labelledby="submission-title"><h2 id="submission-title">{SUBMISSION_LABELS[type]}</h2>{type==="resignation"?<p>送信後、事務から連絡します。</p>:null}{type==="nda"?<div className={styles.pledge}>業務上知り得た秘密および個人情報を、在職中および退職後も第三者へ漏らしません。</div>:null}<form onSubmit={submit}>{definitions[type].map(([key,label,input,optional])=><label key={key}>{label}{input==="textarea"?<textarea value={String(values[key]??"")} onChange={e=>setValues({...values,[key]:e.target.value})}/>:<input type={input} required={!optional} value={String(values[key]??"")} onChange={e=>setValues({...values,[key]:e.target.value})}/>}</label>)}{type==="bank_account"?<p>種別は「普通」で受け付けます。</p>:null}{type==="nda"?<label className={styles.agree}><input type="checkbox" checked={values.agreed===true} onChange={e=>setValues({...values,agreed:e.target.checked})}/>内容に同意します</label>:null}{error?<p role="alert" className={styles.error}>{error}</p>:null}<div className={styles.modalActions}><button type="submit" disabled={sending}>{sending?"送信中…":"送信する"}</button><button type="button" className={styles.secondaryButton} onClick={onClose}>閉じる</button></div></form></section></div>}
