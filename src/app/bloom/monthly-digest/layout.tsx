import type { ReactNode } from "react";

export const metadata = {
  title: "月次ダイジェスト — Garden Bloom",
};

export default function MonthlyDigestLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
