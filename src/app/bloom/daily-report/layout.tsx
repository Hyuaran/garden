import type { ReactNode } from "react";

export const metadata = {
  title: "日報 — Garden Bloom",
};

export default function DailyReportLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
