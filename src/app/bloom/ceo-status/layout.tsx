import type { ReactNode } from "react";

export const metadata = {
  title: "経営状況 — Garden Bloom",
};

export default function CeoStatusLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
