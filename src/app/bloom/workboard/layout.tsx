import type { ReactNode } from "react";

export const metadata = {
  title: "ワークボード — Garden Bloom",
};

export default function WorkboardLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
