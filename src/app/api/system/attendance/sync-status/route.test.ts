import {beforeEach,describe,expect,it,vi} from "vitest";const mocks=vi.hoisted(()=>({resolve:vi.fn(),admin:vi.fn()}));vi.mock("../_lib/auth",()=>({resolveAttendanceEmployee:mocks.resolve}));vi.mock("@/lib/supabase/admin",()=>({getSupabaseAdmin:mocks.admin}));import{GET}from"./route";
describe("GET attendance sync-status",()=>{beforeEach(()=>mocks.admin.mockReset());it("rejects staff before creating admin client",async()=>{mocks.resolve.mockResolvedValue({ok:true,employee:{id:"EMP-0001",name:"A",gardenRole:"staff"}});expect((await GET()).status).toBe(403);expect(mocks.admin).not.toHaveBeenCalled();});it.each(["manager","admin","super_admin"])("allows %s",async(role)=>{mocks.resolve.mockResolvedValue({ok:true,employee:{id:"EMP-0001",name:"A",gardenRole:role}});let calls=0;const from=()=>{calls++;if(calls<=4){const countChain={select(){return this},eq:async()=>({count:calls===1?3:0,error:null})};return countChain;}return{select(){return this},in(){return this},order(){return this},limit:async()=>({data:[{id:1,kot_sync_status:"unsent"}],error:null})};};mocks.admin.mockReturnValue({from});const response=await GET();expect(response.status).toBe(200);expect(await response.json()).toMatchObject({counts:{unsent:3,failed:0},limit:200});});});
