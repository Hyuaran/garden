import {NextResponse} from "next/server";import {getSupabaseAdmin} from "@/lib/supabase/admin";import {requireEmployee} from "@/app/system/mypage/_lib/submission-server";import {SUBMISSION_LABELS,validateSubmission,type SubmissionType} from "@/app/system/mypage/_lib/submission-types";
export async function GET(){const employee=await requireEmployee();if(!employee)return NextResponse.json({ok:false},{status:401});const {data,error}=await getSupabaseAdmin().from("system_mypage_submissions").select("id,submission_type,payload,status,proposed_one_way,created_at").eq("employee_id",employee.employee_id).order("created_at",{ascending:false}).limit(30);return error?NextResponse.json({ok:false},{status:500}):NextResponse.json({ok:true,rows:data});}
export async function POST(request:Request){const employee=await requireEmployee();if(!employee)return NextResponse.json({ok:false},{status:401});let body:{type?:SubmissionType;payload?:Record<string,unknown>}={};try{body=await request.json()}catch{return NextResponse.json({ok:false,error:"入力内容を確認してください。"},{status:400})}if(!body.type||!(body.type in SUBMISSION_LABELS)||!body.payload)return NextResponse.json({ok:false,error:"届出の種類を確認してください。"},{status:400});const error=validateSubmission(body.type,body.payload);if(error)return NextResponse.json({ok:false,error},{status:400});const payload={...body.payload,...(body.type==="nda"?{signedAt:new Date().toISOString()}: {})};const {data, error:dbError}=await getSupabaseAdmin().from("system_mypage_submissions").insert({employee_id:employee.employee_id,submission_type:body.type,payload,status:"received",kintone_status:["commute_route","bank_account"].includes(body.type)?"pending":"not_applicable"}).select("id").single();return dbError?NextResponse.json({ok:false,error:"送信できませんでした。"},{status:500}):NextResponse.json({ok:true,id:data.id},{status:201});}
